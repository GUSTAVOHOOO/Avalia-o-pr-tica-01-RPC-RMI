---
phase: 05-exchange-spy-mechanics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_exchange.py
autonomous: true
requirements:
  - EXCHANGE-01
  - EXCHANGE-02
  - EXCHANGE-03
  - EXCHANGE-04
  - EXCHANGE-05
  - EXCHANGE-06
  - SPY-01
  - SPY-02
  - SPY-03
  - SPY-04
  - SPY-05

must_haves:
  truths:
    - "tests/test_exchange.py exists with 15 test functions"
    - "All 15 tests skip cleanly via pytest.skip (no import errors, no failures)"
    - "FakeBroadcaster and _server_with_exchange_state helper are defined inline in the file"
    - "Each test function name matches the exact name referenced in VALIDATION.md"
  artifacts:
    - path: "tests/test_exchange.py"
      provides: "15 skipping test stubs for all Phase 5 requirements"
      min_lines: 80
  key_links:
    - from: "tests/test_exchange.py"
      to: "server/game_server.py"
      via: "GameServer import at top of file"
      pattern: "from server.game_server import GameServer"
---

<objective>
Wave 0: Create the test scaffold for Phase 5 before any implementation begins.

Purpose: Nyquist rule compliance — all 15 unit tests must exist (skipping) before implementation tasks can reference them. Tests define the behavioral contract that subsequent plans implement against.

Output: tests/test_exchange.py with 15 test stubs, FakeBroadcaster, and _server_with_exchange_state helper; all stubs skip cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-exchange-spy-mechanics/05-CONTEXT.md
@.planning/phases/05-exchange-spy-mechanics/05-RESEARCH.md
@.planning/phases/05-exchange-spy-mechanics/05-VALIDATION.md

<interfaces>
<!-- Existing pattern extracted from tests/test_turn_state.py — replicate this exactly. -->

FakeBroadcaster (from tests/test_turn_state.py):
```python
class FakeBroadcaster:
    def __init__(self):
        self.events = []

    def broadcast(self, event_type, data, exclude=None):
        self.events.append({"type": event_type, "data": data})

    def send_to_player(self, player_id, event_type, data):
        self.events.append({"type": event_type, "player_id": player_id, "data": data})
```

_server_with_turn_state helper (from tests/test_turn_state.py) — adapt to EXCHANGE_PHASE:
- Creates a GameServer with Alice (host) and Bob (player)
- Replaces broadcaster with FakeBroadcaster
- Sets turn_machine.current_phase = "EXCHANGE_PHASE"
- Sets turn_machine.current_turn_state with image_assignments
- Returns (server, session, host_player_id, other_player_id)

pytest.skip pattern from prior phases:
```python
def test_something():
    pytest.skip("stub — implement in plan 02")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_exchange.py with 15 skipping stubs</name>
  <files>tests/test_exchange.py</files>
  <read_first>
    - tests/test_turn_state.py (FakeBroadcaster class, _server_with_turn_state helper, pytest.skip stub pattern — replicate both exactly)
    - .planning/phases/05-exchange-spy-mechanics/05-VALIDATION.md (exact test function names in Per-Task Verification Map)
    - .planning/phases/05-exchange-spy-mechanics/05-RESEARCH.md (Phase Requirements → Test Map section for all 15 test names)
  </read_first>
  <action>
    Create tests/test_exchange.py. The file must contain exactly these elements in this order:

    1. Module docstring: "Unit tests for Phase 5 exchange and spy mechanics."

    2. Imports: import pytest; from server.game_server import GameServer; from server.turn_machine import TurnMachine; from server.turn_state import TurnState.

    3. FakeBroadcaster class — copy verbatim from tests/test_turn_state.py (same __init__, broadcast, send_to_player signatures and bodies).

    4. _server_with_exchange_state(phase="EXCHANGE_PHASE") helper function — adapt _server_with_turn_state from tests/test_turn_state.py. This helper must: call server.create_game("Alice", "PYRO:fake.alice@127.0.0.1:9999", 3), call server.join_game("Bob", "PYRO:fake.bob@127.0.0.1:9999", room_code), replace server.broadcaster with FakeBroadcaster(), create a TurnMachine with room_code and player_ids, set turn_machine.current_phase = phase, create TurnState with turn_number=1 and image_assignments for both players, assign turn_machine.current_turn_state, and return (server, session, host_player_id, join_player_id). The helper also needs a third player "Charlie" for spy tests — add charlie = server.join_game("Charlie", "PYRO:fake.charlie@127.0.0.1:9999", room_code) and include charlie["player_id"] in return tuple as a fifth element.

    5. The following 15 test functions, each body is exactly: pytest.skip("stub — implement in plan 02") or "stub — implement in plan 03" as appropriate. Use exact names:
       - test_request_exchange (EXCHANGE-01)
       - test_request_exchange_wrong_phase (EXCHANGE-01)
       - test_respond_exchange_accept (EXCHANGE-02)
       - test_respond_exchange_reject (EXCHANGE-02)
       - test_submit_exchange_hint_completes (EXCHANGE-03)
       - test_exchange_completed_payload (EXCHANGE-04)
       - test_private_hints_delivered (EXCHANGE-05)
       - test_exchange_one_per_turn (EXCHANGE-06)
       - test_spy_phase_skipped_when_no_exchanges (D-06)
       - test_spy_phase_entered_when_exchange_exists (D-06)
       - test_spy_wrong_phase (SPY-01)
       - test_spy_discovery_probability (SPY-02)
       - test_spy_success_private (SPY-03)
       - test_spy_own_exchange_rejected (SPY-04)
       - test_spy_one_per_turn (SPY-05)

    Stubs for tests_request_exchange through test_exchange_one_per_turn and both D-06 stubs: use "stub — implement in plan 02". Stubs for test_spy_wrong_phase through test_spy_one_per_turn: use "stub — implement in plan 03".
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_exchange.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_exchange.py exists and contains exactly 15 test functions
    - Running `python -m pytest tests/test_exchange.py -x -q` reports "15 skipped" with 0 failures and 0 errors
    - File contains FakeBroadcaster class with broadcast() and send_to_player() methods
    - File contains _server_with_exchange_state() returning a 5-tuple (server, session, host_id, join_id, charlie_id)
    - All 15 exact function names listed in the action are present (grep test_request_exchange, test_respond_exchange_accept, test_spy_one_per_turn, etc.)
    - `python -m pytest tests/ -q` still passes all prior tests (34 existing tests green, 15 new skipped)
  </acceptance_criteria>
  <done>tests/test_exchange.py exists; `python -m pytest tests/test_exchange.py -x -q` reports 15 skipped, 0 failed; full suite `python -m pytest tests/ -q` shows 34 passed + 15 skipped, 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → GameServer | Tests call GameServer methods directly in-process — no RPC boundary in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00 | Tampering | test stubs | accept | Stubs use pytest.skip — they cannot be accidentally merged as passing tests; CI would show "skipped" not "passed" |
</threat_model>

<verification>
Run after task completes:
- `python -m pytest tests/test_exchange.py -x -q` → 15 skipped, 0 failed
- `python -m pytest tests/ -q` → all prior tests pass + 15 new skipped, 0 errors
</verification>

<success_criteria>
Wave 0 complete when:
- tests/test_exchange.py has 15 skipping test stubs with correct function names
- FakeBroadcaster and _server_with_exchange_state helpers defined
- Full test suite green (0 failures, 0 errors)
- All 11 requirement IDs are represented across the stubs
</success_criteria>

<output>
After completion, create `.planning/phases/05-exchange-spy-mechanics/05-01-SUMMARY.md`
</output>
