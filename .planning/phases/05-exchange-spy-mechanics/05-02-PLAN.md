---
phase: 05-exchange-spy-mechanics
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - server/turn_state.py
  - server/turn_machine.py
autonomous: true
requirements:
  - EXCHANGE-01
  - EXCHANGE-02
  - EXCHANGE-03
  - EXCHANGE-04
  - EXCHANGE-05
  - EXCHANGE-06

must_haves:
  truths:
    - "ExchangeRecord dataclass exists in server/turn_state.py with all 5 required fields"
    - "TurnState has 4 new fields: exchanges, completed_exchanges, exchange_participants, spy_attempts"
    - "_compute_next('EXCHANGE_PHASE') returns 'SCORING_PHASE' when completed_exchanges is empty"
    - "_compute_next('EXCHANGE_PHASE') returns 'SPY_PHASE' when completed_exchanges is non-empty"
    - "PHASE_CHANGED broadcast for SPY_PHASE includes spy_targets list"
    - "Tests test_spy_phase_skipped_when_no_exchanges and test_spy_phase_entered_when_exchange_exists pass"
  artifacts:
    - path: "server/turn_state.py"
      provides: "ExchangeRecord dataclass + 4 new TurnState fields"
      contains: "class ExchangeRecord"
    - path: "server/turn_machine.py"
      provides: "SPY_PHASE conditional skip in _compute_next + spy_targets in broadcast"
      contains: "completed_exchanges"
  key_links:
    - from: "server/game_server.py"
      to: "server/turn_state.py"
      via: "ExchangeRecord import (added in plan 03)"
      pattern: "from server.turn_state import TurnState"
    - from: "server/turn_machine.py"
      to: "server/turn_state.py"
      via: "TurnState instantiation in _advance_to(HINT_PHASE)"
      pattern: "TurnState("
---

<objective>
Wave 1: Extend the data layer for Phase 5 mechanics.

Purpose: Add ExchangeRecord dataclass and 4 new TurnState fields (per D-03), then modify _compute_next() in TurnMachine to skip SPY_PHASE when no completed exchanges exist (per D-06). Also extend _advance_to() to include spy_targets list in PHASE_CHANGED payload when entering SPY_PHASE.

Output: server/turn_state.py with ExchangeRecord + new TurnState fields; server/turn_machine.py with SPY_PHASE skip logic; tests for D-06 passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-exchange-spy-mechanics/05-CONTEXT.md
@.planning/phases/05-exchange-spy-mechanics/05-RESEARCH.md

<interfaces>
<!-- Extracted from server/turn_state.py (current state — the file to extend) -->

Current TurnState fields (from turn_state.py):
```python
@dataclasses.dataclass
class TurnState:
    turn_number: int
    player_ids: list
    hints_submitted: dict = dataclasses.field(default_factory=dict)
    guesses_made: dict = dataclasses.field(default_factory=dict)
    correct_guesses: list = dataclasses.field(default_factory=list)
    image_assignments: dict = dataclasses.field(default_factory=dict)
```

Current _compute_next() in turn_machine.py (the method to extend):
```python
def _compute_next(self, current_phase: str) -> str:
    with self.lock:
        if current_phase == "TURN_END":
            if self.current_turn >= self.max_turns:
                return "GAME_ENDED"
            self.current_turn += 1
            return "HINT_PHASE"
        idx = PHASE_SEQUENCE.index(current_phase)
        return PHASE_SEQUENCE[idx + 1]
```

_advance_to() in turn_machine.py — the spy_targets injection point (after GUESS_PHASE hints injection):
```python
if phase == "GUESS_PHASE" and self.current_turn_state is not None:
    broadcast_data["hints"] = dict(self.current_turn_state.hints_submitted)
# ADD analogous block for SPY_PHASE here
```

ExchangeRecord target shape (per D-03 and RESEARCH.md Pattern 1):
- requester_id: str
- target_id: str
- status: str = "pending"   # one of: "pending", "accepted", "rejected", "completed"
- requester_hint: Optional[str] = None
- target_hint: Optional[str] = None

New TurnState fields (per D-03 and RESEARCH.md Pattern 2):
- exchanges: dict = dataclasses.field(default_factory=dict)              # exchange_id -> ExchangeRecord
- completed_exchanges: list = dataclasses.field(default_factory=list)    # ordered list of exchange_ids
- exchange_participants: set = dataclasses.field(default_factory=set)    # player_ids that used their slot
- spy_attempts: set = dataclasses.field(default_factory=set)             # player_ids that used their spy slot
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ExchangeRecord dataclass and new TurnState fields</name>
  <files>server/turn_state.py</files>
  <read_first>
    - server/turn_state.py (current state — all existing fields and methods to preserve)
    - .planning/phases/05-exchange-spy-mechanics/05-RESEARCH.md (Pattern 1 and Pattern 2 sections for exact field specs)
    - .planning/phases/05-exchange-spy-mechanics/05-CONTEXT.md (D-03 for field names and types)
  </read_first>
  <behavior>
    - ExchangeRecord("p1", "p2").status == "pending"
    - ExchangeRecord("p1", "p2").requester_hint is None
    - ExchangeRecord("p1", "p2").target_hint is None
    - TurnState(1, ["p1"]).exchanges == {}
    - TurnState(1, ["p1"]).completed_exchanges == []
    - TurnState(1, ["p1"]).exchange_participants == set()
    - TurnState(1, ["p1"]).spy_attempts == set()
    - Existing all_hints_submitted() method still works correctly
  </behavior>
  <action>
    In server/turn_state.py:

    1. Add `from typing import Optional` to imports (after `import dataclasses`).

    2. Before the TurnState class definition, insert ExchangeRecord dataclass:
       - Decorator: @dataclasses.dataclass
       - Class name: ExchangeRecord
       - Docstring: "Tracks one 1-on-1 private hint exchange within a turn."
       - Fields in this exact order:
         - requester_id: str
         - target_id: str
         - status: str = "pending"
         - requester_hint: Optional[str] = None
         - target_hint: Optional[str] = None

    3. In TurnState, add 4 new fields AFTER the existing image_assignments field, using the same dataclasses.field(default_factory=...) pattern:
       - exchanges: dict = dataclasses.field(default_factory=dict)
       - completed_exchanges: list = dataclasses.field(default_factory=list)
       - exchange_participants: set = dataclasses.field(default_factory=set)
       - spy_attempts: set = dataclasses.field(default_factory=set)

    4. Add inline comment after each new field referencing its purpose per D-03:
       - exchanges: "# exchange_id -> ExchangeRecord (D-03)"
       - completed_exchanges: "# ordered list of completed exchange_ids, spy target list (D-03)"
       - exchange_participants: "# player_ids that used their exchange slot (D-03, EXCHANGE-06)"
       - spy_attempts: "# player_ids that used their spy slot (D-03, SPY-05)"

    5. Do NOT modify or remove any existing field or method. all_hints_submitted() must remain unchanged.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -c "from server.turn_state import TurnState, ExchangeRecord; ts = TurnState(1, []); er = ExchangeRecord('p1', 'p2'); print(er.status, er.requester_hint, er.target_hint, ts.exchanges, ts.completed_exchanges, ts.exchange_participants, ts.spy_attempts)"</automated>
  </verify>
  <acceptance_criteria>
    - `from server.turn_state import TurnState, ExchangeRecord` succeeds with no ImportError
    - `ExchangeRecord("p1", "p2").status` == "pending"
    - `ExchangeRecord("p1", "p2").requester_hint` is None
    - `ExchangeRecord("p1", "p2").target_hint` is None
    - `TurnState(1, []).exchanges` == {}
    - `TurnState(1, []).completed_exchanges` == []
    - `TurnState(1, []).exchange_participants` == set()
    - `TurnState(1, []).spy_attempts` == set()
    - `TurnState(1, ["p1"]).all_hints_submitted()` returns False (method still works)
    - `python -m pytest tests/test_turn_state.py -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>ExchangeRecord dataclass and 4 new TurnState fields added; all existing tests still pass; ExchangeRecord is importable from server.turn_state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SPY_PHASE skip logic to _compute_next and spy_targets to _advance_to</name>
  <files>server/turn_machine.py</files>
  <read_first>
    - server/turn_machine.py (current _compute_next() and _advance_to() implementations — understand the exact insertion points)
    - .planning/phases/05-exchange-spy-mechanics/05-RESEARCH.md (Pattern 3 for _compute_next() addition; Code Examples section for spy_targets injection in _advance_to())
    - .planning/phases/05-exchange-spy-mechanics/05-CONTEXT.md (D-06 for the skip condition; Pitfall 3 for the None guard)
    - tests/test_exchange.py (test_spy_phase_skipped_when_no_exchanges and test_spy_phase_entered_when_exchange_exists — the tests to make pass)
  </read_first>
  <behavior>
    - _compute_next("EXCHANGE_PHASE") with empty completed_exchanges returns "SCORING_PHASE"
    - _compute_next("EXCHANGE_PHASE") with one exchange_id in completed_exchanges returns "SPY_PHASE"
    - _compute_next("EXCHANGE_PHASE") with current_turn_state = None returns "SCORING_PHASE" (None guard)
    - _compute_next("TURN_END") still returns "GAME_ENDED" or "HINT_PHASE" (no regression)
    - _compute_next("HINT_PHASE") still returns "GUESS_PHASE" (no regression)
    - PHASE_CHANGED broadcast data for SPY_PHASE includes "spy_targets" key with list of exchange_ids
  </behavior>
  <action>
    Modification 1 — _compute_next() in server/turn_machine.py:

    In _compute_next(), inside the `with self.lock:` block, add a new conditional branch BEFORE the existing `idx = PHASE_SEQUENCE.index(current_phase)` line:

    ```
    if current_phase == "EXCHANGE_PHASE":
        ts = self.current_turn_state
        if ts is None or not ts.completed_exchanges:
            return "SCORING_PHASE"
        return "SPY_PHASE"
    ```

    This implements D-06. The branch must come before the generic PHASE_SEQUENCE index lookup so EXCHANGE_PHASE never falls through to the default branch.

    Modification 2 — _advance_to() in server/turn_machine.py:

    In _advance_to(), inside the `with self.lock:` block, after the existing GUESS_PHASE block that injects `broadcast_data["hints"]`, add a new analogous block for SPY_PHASE:

    ```
    if phase == "SPY_PHASE" and self.current_turn_state is not None:
        broadcast_data["spy_targets"] = list(self.current_turn_state.completed_exchanges)
    ```

    This makes `PHASE_CHANGED` payloads for SPY_PHASE include the spy target list (Claude's Discretion recommendation from CONTEXT.md).

    Do NOT modify PHASE_SEQUENCE. Do NOT modify _advance_to() in any other way. Do NOT modify advance_phase_manual() or advance_to_guess_phase().
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_exchange.py::test_spy_phase_skipped_when_no_exchanges tests/test_exchange.py::test_spy_phase_entered_when_exchange_exists -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_exchange.py::test_spy_phase_skipped_when_no_exchanges -x -q` exits 0 (passes, not skipped)
    - `python -m pytest tests/test_exchange.py::test_spy_phase_entered_when_exchange_exists -x -q` exits 0 (passes, not skipped)
    - `python -m pytest tests/test_turn_state.py -q` exits 0 (no regressions in Phase 4 tests)
    - `grep "EXCHANGE_PHASE" server/turn_machine.py` shows the new conditional branch
    - `grep "spy_targets" server/turn_machine.py` shows the _advance_to() injection
    - _compute_next() docstring updated to mention the EXCHANGE_PHASE branch (D-06)
  </acceptance_criteria>
  <done>_compute_next() skips SPY_PHASE when completed_exchanges is empty; spy_targets injected into PHASE_CHANGED broadcast; both D-06 tests pass; no Phase 4 test regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TurnMachine → TurnState | _compute_next() reads turn_state under lock — no external input crosses this boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | TurnState.exchange_participants | mitigate | Set field is mutated only under GameServer.lock (RLock); Python sets are not thread-safe without the lock — all mutations in plan 03 RPC methods will hold self.lock |
| T-05-02 | Denial of Service | _compute_next() None guard | mitigate | `if ts is None or not ts.completed_exchanges` prevents AttributeError when current_turn_state is None; None check is cheap insurance per RESEARCH.md Pitfall 3 |
</threat_model>

<verification>
Run after both tasks complete:
- `python -m pytest tests/test_exchange.py -x -q` → 2 passing (D-06 tests), 13 skipped, 0 failed
- `python -m pytest tests/test_turn_state.py -q` → 18 passed, 0 failed
- `python -m pytest tests/ -q` → all prior tests pass, 2 new exchange tests pass, 13 skipped
</verification>

<success_criteria>
Wave 1 complete when:
- ExchangeRecord dataclass importable from server.turn_state with 5 correct fields
- TurnState has 4 new fields with correct default_factory values
- _compute_next("EXCHANGE_PHASE") conditionally returns "SCORING_PHASE" or "SPY_PHASE" based on completed_exchanges
- PHASE_CHANGED broadcast for SPY_PHASE includes spy_targets list
- Both D-06 test stubs converted to passing tests
- Zero regressions in test_turn_state.py
</success_criteria>

<output>
After completion, create `.planning/phases/05-exchange-spy-mechanics/05-02-SUMMARY.md`
</output>
