---
phase: 02-player-session-lobby
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/game_server.py
  - config.py
  - tests/test_session.py
autonomous: true
requirements:
  - SESSION-01
  - SESSION-02
  - SESSION-03
  - SESSION-04
  - SESSION-05
  - SESSION-06

must_haves:
  truths:
    - "create_game() stores a GameSession and returns {player_id, room_code, is_host: True}"
    - "room_code is exactly 6 uppercase alphanumeric characters; two calls return different codes"
    - "join_game() appends a player to an existing WAITING session and registers their callback"
    - "join_game() returns {error: 'jogo em andamento'} when session status is not WAITING"
    - "start_game() returns True only when caller is host AND session has ≥2 players; False otherwise"
    - "join_game() triggers PLAYER_JOINED broadcast to all registered callbacks"
  artifacts:
    - path: "server/game_server.py"
      provides: "GameServer with create_game, join_game, start_game, leave_game, GameSession/PlayerInfo dataclasses"
      exports: ["GameServer", "GameSession", "PlayerInfo"]
    - path: "tests/test_session.py"
      provides: "6 unit tests covering SESSION-01 through SESSION-06"
      contains: "test_create_game, test_room_code_format, test_join_game, test_join_rejected_if_started, test_player_joined_broadcast, test_start_game_validation"
    - path: "config.py"
      provides: "FRONTEND_DIST_PATH constant"
      contains: "FRONTEND_DIST_PATH"
  key_links:
    - from: "server/game_server.py create_game"
      to: "server/event_broadcaster.py register_callback"
      via: "self.broadcaster.register_callback(player_id, callback_uri)"
      pattern: "broadcaster\\.register_callback"
    - from: "server/game_server.py join_game"
      to: "server/event_broadcaster.py broadcast"
      via: "self.broadcaster.broadcast('player_joined', broadcast_data) outside lock"
      pattern: "broadcaster\\.broadcast.*player_joined"
---

<objective>
Implement the GameServer session layer: GameSession/PlayerInfo dataclasses, create_game, join_game, start_game, and leave_game methods, plus unit tests covering SESSION-01 through SESSION-06.

Purpose: This is the authoritative backend state machine for all session management. The bridge (Plan 02) calls these methods via Pyro5 RPC. No UI work here — pure Python tested in isolation.

Output:
- Extended server/game_server.py with session methods
- tests/test_session.py with 6 passing unit tests
- config.py extended with FRONTEND_DIST_PATH
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-player-session-lobby/02-CONTEXT.md
@.planning/phases/02-player-session-lobby/02-RESEARCH.md
@.planning/phases/02-player-session-lobby/02-PATTERNS.md

<interfaces>
<!-- Current state of files being extended — executor must read these before modifying -->

From server/game_server.py (current Phase 1 state):
```python
@Pyro5.api.expose
class GameServer:
    def __init__(self):
        self.lock = threading.RLock()
        self.broadcaster = EventBroadcaster()

    def ping(self) -> str: ...
    def register_callback(self, player_id: str, callback_uri: str) -> bool: ...

    @Pyro5.api.oneway
    def broadcast_test(self, message: str) -> None: ...
```

From server/event_broadcaster.py (Phase 1, do not modify):
```python
class EventBroadcaster:
    def register_callback(self, player_id: str, callback_uri: str) -> None: ...
    def broadcast(self, event_type: str, data: dict) -> None: ...  # iterates by player_id key
```

From config.py (current):
```python
NS_HOST = os.environ.get("PYRO_NS_HOST", "127.0.0.1")
GAME_SERVER_PORT = 9091
BRIDGE_PORT = 5000
GAME_SERVER_NAME = "game.server"
```

From tests/test_unit.py (analog for test_session.py):
```python
def _start_daemon(obj, object_id: str):
    daemon = Pyro5.api.Daemon(host="127.0.0.1")
    uri = daemon.register(obj, objectId=object_id)
    t = threading.Thread(target=daemon.requestLoop, daemon=True)
    t.start()
    return daemon, str(uri)

def test_ping():
    server = GameServer()
    daemon, uri = _start_daemon(server, "test.game.server.ping")
    try:
        with Pyro5.api.Proxy(uri) as proxy:
            result = proxy.ping()
        assert result == "pong"
    finally:
        daemon.shutdown()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GameSession dataclasses + create_game + join_game + test_session.py scaffold</name>
  <files>server/game_server.py, tests/test_session.py, config.py</files>
  <read_first>
    - server/game_server.py — current full file; understand imports, class structure, lock pattern, input validation pattern
    - server/event_broadcaster.py — confirm broadcast() and register_callback() signatures; verify iteration is by player_id key (not URI deduplication)
    - tests/test_unit.py — copy _start_daemon() helper, test structure, callback receiver inline pattern, broadcast delivery wait pattern verbatim
    - .planning/phases/02-player-session-lobby/02-RESEARCH.md §Pattern 1 through Pattern 3, §Code Examples "GameSession State Model" and "join_game with Broadcast After Lock Release"
    - .planning/phases/02-player-session-lobby/02-PATTERNS.md §server/game_server.py — imports pattern, class declaration pattern, lock pattern
  </read_first>

  <behavior>
    - test_create_game: calls create_game("Alice", fake_cb_uri, 5) via Pyro5 proxy; asserts return is dict with keys player_id (non-empty str), room_code (6-char string), is_host=True; asserts server.sessions has room_code key with status="WAITING" and 1 player
    - test_room_code_format: calls create_game twice; asserts both room_codes match r"^[A-Z0-9]{6}$"; asserts the two codes are different
    - test_join_game: calls create_game to create session, then join_game("Bob", fake_cb_uri, room_code); asserts return dict has player_id, room_code matching original, is_host=False; asserts session.players has 2 entries
    - test_join_rejected_if_started: creates session, manually sets session.status="IN_PROGRESS" on server object, calls join_game; asserts return has key "error" with value "jogo em andamento"
    - test_player_joined_broadcast: creates session with host using fake_cb_uri pointing to inline TestCallbackReceiver; second player joins via join_game; receiver.event.wait(timeout=5) returns True; receiver.received has at least 1 entry with room_code key
    - test_start_game_validation: creates session with 1 player; asserts start_game(player_id, 5) returns False; joins second player; asserts start_game(host_player_id, 5) returns True; asserts start_game(non_host_player_id, 5) returns False
  </behavior>

  <action>
    Write tests first (RED phase), then implement (GREEN phase).

    In tests/test_session.py:
    - Copy _start_daemon() helper verbatim from tests/test_unit.py
    - For test_player_joined_broadcast: define inline TestCallbackReceiver class inside the test function decorated @Pyro5.api.expose, with on_player_joined(self, data) method that appends to self.received and calls self.event.set(); start its daemon with _start_daemon; use that URI as callback_uri in create_game/join_game calls
    - Use unique object_id per test (e.g., "test.gs.create", "test.gs.join", etc.) to avoid port conflicts
    - Each test: finally: daemon.shutdown()

    In server/game_server.py add before the GameServer class:
    - imports: uuid, random, string, dataclasses, from typing import List
    - PlayerInfo dataclass: fields player_id: str, player_name: str, callback_uri: str, is_host: bool
    - GameSession dataclass: fields room_code: str, host_id: str, max_turns: int, status: str (WAITING/IN_PROGRESS/ENDED), players: List[PlayerInfo] (default_factory=list); property player_count returning len(self.players); method get_player_dicts() returning list of {player_id, player_name, is_host}

    In GameServer.__init__ add: self.sessions: dict[str, GameSession] = {}

    Add _generate_room_code(self) -> str — must be called while self.lock is held; chars = string.ascii_uppercase + string.digits; while loop generating 6-char code with random.choices(chars, k=6) until code not in self.sessions

    Add create_game(self, player_name: str, callback_uri: str, max_turns: int) -> dict:
    - Validate player_name non-empty str, max 50 chars; callback_uri non-empty str; max_turns in {3,5,7,10} — raise ValueError on invalid inputs
    - Under self.lock: generate room_code via _generate_room_code(); player_id = str(uuid.uuid4()); create PlayerInfo with is_host=True; create GameSession with status="WAITING"; add to self.sessions; call self.broadcaster.register_callback(player_id, callback_uri)
    - Return {"player_id": player_id, "room_code": room_code, "is_host": True}
    - Do NOT broadcast inside the lock (no PLAYER_JOINED on create — first player has no one to notify)

    Add join_game(self, player_name: str, callback_uri: str, room_code: str) -> dict:
    - Validate player_name non-empty str max 50 chars; callback_uri non-empty str; room_code non-empty str
    - Under self.lock: look up session = self.sessions.get(room_code); if None return {"error": "sala nao encontrada"}; if session.status != "WAITING" return {"error": "jogo em andamento"}; if session.player_count >= 6 return {"error": "sala cheia"}; player_id = str(uuid.uuid4()); append PlayerInfo with is_host=False; call self.broadcaster.register_callback(player_id, callback_uri); snapshot broadcast_data = {"room_code": room_code, "player": {"player_id": player_id, "player_name": player_name, "is_host": False}, "players": session.get_player_dicts()}
    - OUTSIDE the lock: self.broadcaster.broadcast("player_joined", broadcast_data) — this is the Pitfall 4 fix
    - Return {"player_id": player_id, "room_code": room_code, "is_host": False}

    Add start_game(self, player_id: str, max_turns: int) -> bool (NOT @oneway — returns bool):
    - Under self.lock: find session where player_id is in players list; if not found return False; if session.host_id != player_id return False; if session.player_count < 2 return False; set session.status = "IN_PROGRESS"; snapshot broadcast_data = {"room_code": session.room_code, "players": session.get_player_dicts()}
    - OUTSIDE the lock: self.broadcaster.broadcast("game_started", broadcast_data)
    - Return True

    Add leave_game(self, player_id: str) -> bool:
    - Under self.lock: find session containing player_id; if not found return False; remove player; if session is now empty: del self.sessions[session.room_code] and return True; if leaving player was host and session.status == "WAITING": promote first remaining player (session.players[0]) as new host (set is_host=True, update session.host_id); snapshot broadcast_data for HOST_CHANGED event
    - OUTSIDE the lock: if host changed: self.broadcaster.broadcast("host_changed", broadcast_data)
    - Return True

    In config.py add after GAME_SERVER_NAME:
    FRONTEND_DIST_PATH = os.environ.get("FRONTEND_DIST", os.path.join(os.path.dirname(__file__), "frontend", "dist"))
  </action>

  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python -m pytest tests/test_session.py -x -q 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_session.py exists and contains 6 test functions: test_create_game, test_room_code_format, test_join_game, test_join_rejected_if_started, test_player_joined_broadcast, test_start_game_validation
    - venv/bin/python -m pytest tests/test_session.py -x -q exits 0 with 6 passed
    - server/game_server.py contains "class GameSession" and "class PlayerInfo"
    - server/game_server.py contains "def create_game", "def join_game", "def start_game", "def leave_game", "def _generate_room_code"
    - server/game_server.py contains "self.sessions: dict" in __init__
    - server/game_server.py join_game calls broadcaster.broadcast after the "with self.lock:" block (broadcast outside lock — Pitfall 4)
    - config.py contains "FRONTEND_DIST_PATH"
    - venv/bin/python -m pytest tests/ -q exits 0 (full suite including Phase 1 tests still pass)
  </acceptance_criteria>

  <done>6 session unit tests pass; GameServer has all 4 session methods + 2 dataclasses; full pytest suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge → GameServer (Pyro5 RPC) | All inputs from bridge arrive as deserialized Python values; bridge forwards browser data without additional sanitization |
| Browser → Bridge (Socket.IO) | Browser-supplied player_name, room_code, max_turns arrive as raw strings/ints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | start_game (GameServer) | mitigate | Validate player_id == session.host_id before starting; return False if not host |
| T-02-02 | Tampering | player_name input | mitigate | Server-side: validate non-empty, max 50 chars, raise ValueError for invalid; prevents oversized payload |
| T-02-03 | Tampering | max_turns input | mitigate | Server-side: validate max_turns in {3,5,7,10}; raise ValueError otherwise |
| T-02-04 | Information Disclosure | room_code enumeration | accept | 6-char alphanumeric = 36^6 ≈ 2.18B combinations; at 2–4 concurrent sessions in academic demo, collision/enumeration probability is negligible |
| T-02-05 | Denial of Service | create_game flooding (memory exhaustion) | accept | Academic scope — restart clears sessions; note in README |
| T-02-06 | Elevation of Privilege | join_game after game started | mitigate | Check session.status == "WAITING" before allowing join; return {"error": "jogo em andamento"} |
</threat_model>

<verification>
Full verification command:
```
cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python -m pytest tests/ -q
```
Expected: all tests pass including Phase 1 tests (test_unit.py) and Phase 2 tests (test_session.py).
</verification>

<success_criteria>
- SESSION-01: create_game() returns {player_id, room_code, is_host: True} and stores GameSession — verified by test_create_game
- SESSION-02: room_code is 6 uppercase alphanumeric chars; two calls return different codes — verified by test_room_code_format
- SESSION-03: join_game() returns {player_id, room_code, is_host: False} for valid WAITING session — verified by test_join_game
- SESSION-04: join_game() returns {"error": "jogo em andamento"} when session not WAITING — verified by test_join_rejected_if_started
- SESSION-05: join_game() triggers PLAYER_JOINED broadcast delivered to registered callback — verified by test_player_joined_broadcast
- SESSION-06: start_game() returns True only for host with ≥2 players — verified by test_start_game_validation
- Full pytest suite (tests/) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-player-session-lobby/02-01-SUMMARY.md`
</output>
