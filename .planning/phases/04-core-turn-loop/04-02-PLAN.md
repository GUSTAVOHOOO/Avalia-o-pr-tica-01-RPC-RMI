---
phase: 04-core-turn-loop
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - server/turn_state.py
  - server/turn_machine.py
  - server/game_server.py
  - tests/test_turn_state.py
autonomous: true
requirements:
  - HINT-01
  - HINT-02
  - HINT-03
  - HINT-04
  - GUESS-05
  - IMAGE-01
  - IMAGE-02
  - IMAGE-03

must_haves:
  truths:
    - "TurnState dataclass exists with all four fields from D-03 and all_hints_submitted() method"
    - "TurnMachine creates a fresh TurnState when entering HINT_PHASE (per D-02)"
    - "Image manifest loaded at GameServer startup; each player receives a unique OBJECT_ASSIGNED private event at ROUND_START (per D-13)"
    - "PHASE_CHANGED to GUESS_PHASE includes hints dict revealing all hints at once (per D-06)"
    - "Players who did not submit before HINT_PHASE timer get empty string in hints_submitted (per D-07)"
  artifacts:
    - path: "server/turn_state.py"
      provides: "TurnState dataclass"
      contains: "class TurnState"
    - path: "server/turn_machine.py"
      provides: "Extended TurnMachine with TurnState lifecycle and image assignment hooks"
      contains: "current_turn_state"
    - path: "server/game_server.py"
      provides: "Extended GameSession with accumulated_scores; updated start_game with player_ids; image manifest loading"
      contains: "accumulated_scores"
  key_links:
    - from: "server/turn_machine.py _advance_to()"
      to: "server/turn_state.py TurnState"
      via: "self.current_turn_state = TurnState(turn_number, player_ids) on HINT_PHASE entry"
      pattern: "current_turn_state\\s*="
    - from: "server/game_server.py start_game()"
      to: "server/turn_machine.py TurnMachine"
      via: "TurnMachine(player_ids=[p.player_id for p in session.players], ...) per Pitfall 2"
      pattern: "player_ids="
    - from: "server/turn_machine.py _advance_to(ROUND_START)"
      to: "server/game_server.py _assign_images_for_turn()"
      via: "on_round_start callback fired after PHASE_CHANGED broadcast for ROUND_START; GameServer assigns images and calls send_to_player per player"
      pattern: "on_round_start"
---

<objective>
Implements the server data layer: TurnState dataclass, TurnMachine extension with per-phase hooks (TurnState creation at HINT_PHASE, image assignment at ROUND_START via on_round_start callback, hints revelation at GUESS_PHASE, empty-hint backfill at timer expiry), and GameServer extensions (image manifest loading, accumulated_scores on GameSession, player_ids plumbed into TurnMachine).

This plan delivers the first vertical slice test coverage: after this plan, test_turn_state.py stubs for HINT-01..04, IMAGE-01/02 can be un-skipped and made green.

Purpose: All Phase 4 game mechanics depend on TurnState existing and TurnMachine producing image assignments and hint revelation at the right moments. Nothing in Plans 03–05 can be built without this foundation.

Output:
- server/turn_state.py (new)
- server/turn_machine.py (extended — 3 phase hooks in _advance_to, player_ids param, on_round_start callback, on_scoring_phase callback)
- server/game_server.py (extended — image manifest load, accumulated_scores field, start_game player_ids, _assign_images_for_turn at ROUND_START, _on_scoring_phase callback handler)
- tests/test_turn_state.py (stubs for HINT/IMAGE tasks un-skipped and implemented)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-core-turn-loop/04-CONTEXT.md
@.planning/phases/04-core-turn-loop/04-RESEARCH.md
@.planning/phases/04-core-turn-loop/04-PATTERNS.md
@.planning/phases/04-core-turn-loop/04-01-SUMMARY.md
@server/turn_machine.py
@server/game_server.py
@server/event_broadcaster.py
@tests/test_turn_state.py

<interfaces>
<!-- From server/turn_machine.py (current state) -->
class TurnMachine:
    def __init__(self, room_code: str, max_turns: int, broadcaster, on_game_ended=None)
    def start(self)
    def _advance_to(self, phase: str, from_timer: bool = False, expected_generation: int = -1)
    def _compute_next(self, current_phase: str) -> str
    def advance_phase_manual(self)
    remaining_seconds: int  # property

<!-- From server/game_server.py (current state) -->
@dataclasses.dataclass
class GameSession:
    room_code: str
    host_id: str
    max_turns: int
    status: str
    players: List[PlayerInfo] = field(default_factory=list)
    turn_machine: object = field(default=None, repr=False)

class GameServer:
    def __init__(self)  # creates self.lock, self.broadcaster, self.sessions, self._player_to_room
    def start_game(self, player_id: str) -> bool  # creates TurnMachine inside lock

<!-- From server/event_broadcaster.py -->
def send_to_player(self, player_id: str, event_type: str, data: dict)  # lines 82-98
def broadcast(self, event_type: str, data: dict)

<!-- Phase 4 CONTEXT.md locked decisions -->
# D-01: TurnState lives in server/turn_state.py
# D-02: TurnMachine creates fresh TurnState on HINT_PHASE entry
# D-03: TurnState fields: hints_submitted, guesses_made, correct_guesses, image_assignments
# D-06: PHASE_CHANGED to GUESS_PHASE payload includes hints: {player_id: hint_word}
# D-07: Non-submitters get empty string at HINT_PHASE timer expiry
# D-12: Images in server/images/manifest.json loaded at GameServer.__init__
# D-13: OBJECT_ASSIGNED private event fired at ROUND_START per player
# D-16: accumulated_scores dict on GameSession
# Pitfall 2: TurnMachine needs player_ids at construction — pass from start_game()
# Pitfall 5: on_scoring_phase callback on TurnMachine for GameSession score accumulation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TurnState dataclass and extend TurnMachine with phase hooks</name>
  <files>server/turn_state.py, server/turn_machine.py</files>
  <read_first>
    - server/turn_machine.py — full file: _advance_to() structure (lines 68–147), lock/broadcast-outside-lock ordering, timer callback closure, _compute_next()
    - server/game_server.py — GameSession and PlayerInfo dataclass patterns (lines 34–73) to mirror TurnState style
    - .planning/phases/04-core-turn-loop/04-PATTERNS.md — TurnState shape (section "server/turn_state.py"), _advance_to() extension pseudocode, on_scoring_phase callback injection pattern
    - .planning/phases/04-core-turn-loop/04-CONTEXT.md — D-01 through D-08, D-13, D-15; Pitfalls 2, 4, 5
  </read_first>
  <behavior>
    - TurnState(turn_number=1, player_ids=["p1","p2"]).all_hints_submitted() returns False when hints_submitted is empty
    - TurnState with all player_ids in hints_submitted: all_hints_submitted() returns True
    - TurnMachine with player_ids=["p1","p2"]: after start() then advance to HINT_PHASE, current_turn_state is TurnState instance with player_ids=["p1","p2"]
    - TurnMachine._advance_to("GUESS_PHASE") fills empty string for any player_id not in hints_submitted
    - TurnMachine._advance_to("GUESS_PHASE") sets broadcast_data["hints"] = dict of hints_submitted
    - TurnMachine._advance_to("ROUND_START") fires on_round_start callback after PHASE_CHANGED broadcast; callback triggers image assignment per player
  </behavior>
  <action>
    Create server/turn_state.py as a pure Python module (no Pyro5, no config import). Imports: dataclasses, typing.List, typing.Optional. Define TurnState dataclass with fields matching D-03 exactly: turn_number: int, player_ids: list, hints_submitted: dict (default_factory=dict), guesses_made: dict (default_factory=dict), correct_guesses: list (default_factory=list), image_assignments: dict (default_factory=dict). Add method all_hints_submitted() -> bool returning len(hints_submitted) >= len(player_ids).

    Extend server/turn_machine.py:
    1. Add import at top: from server.turn_state import TurnState
    2. Extend __init__ signature: add player_ids: list parameter (after broadcaster), on_round_start=None parameter, and on_scoring_phase=None parameter (after on_game_ended). Store as self.player_ids = list(player_ids), self._on_round_start = on_round_start, and self._on_scoring_phase = on_scoring_phase. Add self.current_turn_state: Optional[TurnState] = None.
    3. In _advance_to(), inside the `with self.lock:` block, at step 5 (after setting self.current_phase), add phase-specific logic before building broadcast_data:
       - When phase == "HINT_PHASE": create self.current_turn_state = TurnState(turn_number=self.current_turn, player_ids=list(self.player_ids)). No image assignment logic here — images are assigned at ROUND_START via the on_round_start callback (see step 4 below).
       - When phase == "GUESS_PHASE": fill empty strings for non-submitters: for pid in self.current_turn_state.player_ids — if pid not in hints_submitted, set hints_submitted[pid] = "". Then after building broadcast_data dict inside lock, add broadcast_data["hints"] = dict(self.current_turn_state.hints_submitted).
       - When phase == "SCORING_PHASE": capture score_deltas_snapshot = None (scoring logic is in Plan 03). For now just capture the current_turn_state snapshot for outside-lock use.
    4. Add a new parameter on_round_start=None to __init__ — stored as self._on_round_start. When _advance_to("ROUND_START"), after lock exit and after the PHASE_CHANGED broadcast, call self._on_round_start(self.current_turn_state) if set. This callback fires during the ROUND_START phase (the 5-second "Round starting..." pause before HINT_PHASE opens), giving players time to see their object before hint input appears. GameServer uses this callback to assign images and send OBJECT_ASSIGNED to each player (D-13). Note: current_turn_state will be the state from the previous turn at ROUND_START — GameServer._assign_images_for_turn stores assignments into turn_state.image_assignments to be available when TurnState is refreshed at HINT_PHASE entry.
    5. After the outside-lock broadcasts, also call self._on_scoring_phase(self.current_turn_state) if phase == "SCORING_PHASE" and self._on_scoring_phase is not None.

    Critical invariant: all lock state mutations INSIDE `with self.lock:`. All broadcaster and callback calls OUTSIDE (after the with block exits). Do NOT change the generation counter or timer scheduling logic.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_turn_machine.py -x && python -c "from server.turn_state import TurnState; ts=TurnState(1,['p1','p2']); assert not ts.all_hints_submitted(); ts.hints_submitted['p1']='a'; ts.hints_submitted['p2']='b'; assert ts.all_hints_submitted(); print('TurnState OK')" && python -c "from server.turn_machine import TurnMachine; print('TurnMachine import OK')"</automated>
  </verify>
  <done>
    - server/turn_state.py exists; TurnState can be instantiated directly; all_hints_submitted() returns correct boolean
    - server/turn_machine.py imports TurnState without error; TurnMachine.__init__ accepts player_ids, on_round_start, on_scoring_phase params
    - tests/test_turn_machine.py still passes (no regressions from Phase 3)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend GameServer with image bank, accumulated_scores, and TurnMachine wiring</name>
  <files>server/game_server.py, tests/test_turn_state.py</files>
  <read_first>
    - server/game_server.py — full file: __init__ (lines 83–88), GameSession dataclass (lines 44–73), start_game (lines 282–331), _set_session_ended pattern (lines 271–279)
    - server/turn_machine.py — updated __init__ signature (from Task 1 above, now includes player_ids, on_round_start, on_scoring_phase)
    - .planning/phases/04-core-turn-loop/04-PATTERNS.md — GameServer image manifest loading pattern, _player_to_room lookup, accumulated_scores field addition
    - .planning/phases/04-core-turn-loop/04-CONTEXT.md — D-12 (manifest path), D-13 (private delivery at ROUND_START), D-16 (accumulated_scores), Pitfall 3 (used_images_this_game dict)
    - tests/test_turn_state.py — current stubs for test_image_manifest_load and test_object_assigned_payload (un-skip these two)
  </read_first>
  <behavior>
    - GameServer() initializes self._image_manifest from server/images/manifest.json (≥8 entries)
    - GameServer() initializes self._used_images_this_game = {} (dict room_code → set of used filenames)
    - GameSession gains accumulated_scores: dict field initialized to {}
    - start_game() passes player_ids=[p.player_id for p in session.players] to TurnMachine constructor
    - start_game() passes on_round_start=self._assign_images_for_turn callback to TurnMachine (fires at ROUND_START per D-13)
    - _assign_images_for_turn(turn_state) selects unique images from manifest (excluding used), assigns to turn_state.image_assignments, then calls send_to_player for each player with {target_player_id, image_url, object_name}
    - _assign_images_for_turn uses self._used_images_this_game[room_code] (dict[str, set]) to avoid repeating images across turns (Pitfall 3)
    - Image URL format: "/static/images/{filename}" (per D-13)
  </behavior>
  <action>
    In server/game_server.py:

    1. Add to imports: import json (stdlib only — no new packages).

    2. Add to GameSession dataclass (after turn_machine field): accumulated_scores: dict = dataclasses.field(default_factory=dict)  # player_id → total int (D-16).

    3. In GameServer.__init__(), after self._player_to_room = {}: load the image manifest:
       _manifest_path = os.path.join(os.path.dirname(__file__), "images", "manifest.json")
       with open(_manifest_path) as f:
           self._image_manifest: dict = json.load(f)
       self._used_images_this_game: dict = {}  # room_code → set of used filenames (Pitfall 3, dict not bare set)

    4. Add private method _assign_images_for_turn(self, room_code: str, turn_state) called OUTSIDE the lock:
       - With self.lock: get session.players list and snapshot it; get or create self._used_images_this_game[room_code] set (initialize to empty set() if key absent).
       - Available filenames = [f for f in self._image_manifest if f not in used_set]. If fewer than len(players): reset used_set to empty (wrap-around for small banks).
       - Use random.sample(available, len(players)) to pick unique filenames. For each (player_id, filename) pair: set turn_state.image_assignments[player_id] = self._image_manifest[filename]; add filename to used_set (inside lock for mutation, outside lock for the send_to_player calls).
       - After lock: call self.broadcaster.send_to_player(player_id, "object_assigned", {"target_player_id": player_id, "image_url": f"/static/images/{filename}", "object_name": self._image_manifest[filename]}) for each assignment.

    5. In start_game(), update TurnMachine construction inside the `with self.lock:` block:
       - Add player_ids=[p.player_id for p in target_session.players]
       - Add on_round_start=lambda ts: self._assign_images_for_turn(room_code_for_cb, ts)
       - Add on_scoring_phase=lambda ts: self._accumulate_scores(room_code_for_cb, ts) (method stub for Plan 03 — for now _accumulate_scores can be a no-op that just logs)

    6. Add stub _accumulate_scores(self, room_code: str, turn_state) method that logs "scoring callback fired" — Plan 03 will implement the real logic.

    Update tests/test_turn_state.py: un-skip test_image_manifest_load and test_object_assigned_payload (remove the pytest.skip line, add real implementation). Both tests call GameServer() directly — GameServer.__init__ now loads the manifest, so test_image_manifest_load asserts len(server._image_manifest) >= 8. test_object_assigned_payload requires a more complex setup (full game with 2 players, start_game call) — if setup is complex, leave these two stubs skipped with a more descriptive comment and implement them as integration tests in Plan 04's smoke test. Do NOT force them if the test setup requires a running Pyro5 daemon.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_turn_machine.py tests/test_session.py -x && python -c "from server.game_server import GameServer; gs=GameServer(); assert len(gs._image_manifest)>=8; assert isinstance(gs._used_images_this_game, dict); print(f'Manifest loaded: {len(gs._image_manifest)} images, _used_images_this_game is dict OK')"</automated>
  </verify>
  <done>
    - GameServer() constructs without error; gs._image_manifest has ≥8 entries; gs._used_images_this_game is a dict (not a set)
    - GameSession dataclass has accumulated_scores field (verify: from server.game_server import GameSession; s=GameSession("R","h",3,"WAITING"); assert hasattr(s,"accumulated_scores"))
    - TurnMachine in start_game() now receives player_ids, on_round_start, on_scoring_phase
    - tests/test_turn_machine.py and tests/test_session.py still pass (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TurnMachine callbacks | on_round_start and on_scoring_phase are internal GameServer lambdas — no external input crosses this boundary |
| Image manifest loading | manifest.json is a server-controlled file; no client input affects which file is loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing | submit_hint — player_id source | defer | player_id originates from _sid_to_player in bridge (Plan 04); TurnMachine and GameServer never receive player_id from client payload directly |
| T-4-02 | Tampering | hint_word injection | defer | input sanitization (strip/cap) implemented in Plan 03 submit_hint() RPC method |
| T-4-03 | Tampering | self-target guess | defer | submit_guess() rejection of self-target implemented in Plan 03 |
| T-4-04 | Tampering | image path traversal | accept | server/images/manifest.json filenames are server-controlled; no client input selects filenames; send_from_directory protection added in Plan 04 |
</threat_model>

<verification>
After both tasks:

```bash
cd /home/spacko/projects/faculdade/sd-rpc-av-1
python -m pytest tests/ -x
```

Expected: all Phase 1–3 tests pass; Phase 4 stubs SKIPPED (no new FAILED).

```bash
python -c "
from server.turn_state import TurnState
from server.game_server import GameServer, GameSession
ts = TurnState(1, ['p1','p2'])
assert not ts.all_hints_submitted()
ts.hints_submitted['p1'] = 'word1'
ts.hints_submitted['p2'] = 'word2'
assert ts.all_hints_submitted()
gs = GameServer()
assert len(gs._image_manifest) >= 8
assert isinstance(gs._used_images_this_game, dict)
s = GameSession('R','h',3,'WAITING')
assert hasattr(s, 'accumulated_scores')
print('All Plan 02 acceptance checks passed')
"
```
</verification>

<success_criteria>
- server/turn_state.py: TurnState dataclass with all D-03 fields and all_hints_submitted() method
- server/turn_machine.py: accepts player_ids, on_round_start, on_scoring_phase; _advance_to hooks fire at HINT_PHASE (TurnState creation), ROUND_START (on_round_start callback for image assignment per D-13), GUESS_PHASE (hints revelation), SCORING_PHASE without breaking existing tests
- server/game_server.py: _image_manifest loaded at init; _used_images_this_game is a dict (room_code → set); accumulated_scores on GameSession; start_game() passes player_ids and callbacks to TurnMachine
- pytest tests/test_turn_machine.py: all pass (no regressions)
- pytest tests/test_session.py: all pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-turn-loop/04-02-SUMMARY.md`
</output>
