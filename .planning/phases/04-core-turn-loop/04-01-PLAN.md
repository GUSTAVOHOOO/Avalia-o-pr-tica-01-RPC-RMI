---
phase: 04-core-turn-loop
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_turn_state.py
  - tests/test_scoring.py
  - server/images/manifest.json
autonomous: true
requirements:
  - HINT-01
  - HINT-02
  - HINT-03
  - HINT-04
  - GUESS-01
  - GUESS-02
  - GUESS-04
  - GUESS-05
  - IMAGE-01
  - IMAGE-02
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - SCORE-05

must_haves:
  truths:
    - "Test files exist and all tests are marked as skipped (not failing) — pytest exits 0 on the quick-run command"
    - "server/images/manifest.json contains at least 8 entries mapping filename to object_name"
    - "server/images/ directory contains at least 8 image placeholder files referenced in manifest.json"
  artifacts:
    - path: "tests/test_turn_state.py"
      provides: "Skipped test stubs for HINT-01..04, GUESS-01/02/04/05, IMAGE-01/02, SCORE-04/05"
      contains: "pytest.skip"
    - path: "tests/test_scoring.py"
      provides: "Skipped test stubs for SCORE-01, SCORE-02, SCORE-03"
      contains: "pytest.skip"
    - path: "server/images/manifest.json"
      provides: "Image bank manifest with filename-to-object_name mapping"
      contains: "apple"
  key_links:
    - from: "tests/test_turn_state.py"
      to: "server/turn_state.py (not yet created)"
      via: "import — will fail until Plan 02 creates the module; stubs must skip before import"
      pattern: "pytest\\.skip"
    - from: "tests/test_scoring.py"
      to: "server/turn_machine.py _calculate_score_deltas (not yet created)"
      via: "import — will fail until Plan 03; stubs must skip before import"
      pattern: "pytest\\.skip"
---

## Phase Goal

**As a** game developer, **I want to** run the full test suite and get zero failures with clear skip markers, **so that** implementation in subsequent plans can be validated incrementally with no false negatives.

<objective>
Wave 0 creates all test infrastructure and the image bank before any implementation. Tests skip gracefully with `pytest.skip` until the production code they target is implemented in Plans 02 and 03.

Purpose: The Nyquist rule requires every implementation task to have an automated verify command. Those commands reference test functions that must exist before implementation begins. Wave 0 fulfills this prerequisite.

Output:
- tests/test_turn_state.py — 14 skipped stubs for HINT/GUESS/IMAGE/SCORE behavior
- tests/test_scoring.py — 4 skipped stubs for pure scoring function (SCORE-01 through SCORE-03 plus score payload)
- server/images/ — directory with ≥8 image files and manifest.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-turn-loop/04-CONTEXT.md
@.planning/phases/04-core-turn-loop/04-VALIDATION.md
@.planning/phases/04-core-turn-loop/04-PATTERNS.md
@tests/test_turn_machine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stubs for TurnState behavior (test_turn_state.py)</name>
  <files>tests/test_turn_state.py</files>
  <read_first>
    - tests/test_turn_machine.py — exact structural model: FakeBroadcaster class, test function naming with requirement IDs in docstrings, descriptive assert messages, `import config` + direct instantiation pattern
    - .planning/phases/04-core-turn-loop/04-VALIDATION.md — exact test function names and requirement IDs mapping
    - .planning/phases/04-core-turn-loop/04-PATTERNS.md — TurnState dataclass shape (hints_submitted, guesses_made, correct_guesses, image_assignments fields)
  </read_first>
  <behavior>
    - test_submit_hint: submit_hint() stores hint word in TurnState.hints_submitted — HINT-01
    - test_submit_hint_duplicate: second submit_hint() call returns {"error": "already_submitted"} — HINT-01
    - test_hint_received_payload: HINT_RECEIVED broadcast includes hints_count and total_players but NOT the hint word — HINT-02
    - test_hint_empty_on_timer: when HINT_PHASE timer expires, players who did not submit get empty string in hints_submitted — HINT-03
    - test_all_hints_auto_advance: when all players submit hints, GUESS_PHASE phase_changed fires immediately — HINT-04
    - test_submit_guess_correct: correct guess (case-insensitive) returns {"ok": True} and guesser_id appears in correct_guesses — GUESS-01
    - test_skip_guess: skip_guess() returns {"ok": True} and guesses_made[player_id] is None — GUESS-02
    - test_guess_result_broadcast: GUESS_RESULT broadcast includes is_correct boolean and guesser_id — GUESS-04
    - test_guess_one_per_turn: second submit_guess() call returns {"error": "already_guessed"} — GUESS-05
    - test_guess_no_self_target: submit_guess where target_player == guesser returns {"error": "cannot_guess_own_object"} — GUESS-05 + D-10
    - test_image_manifest_load: GameServer initializes with image_manifest dict loaded from server/images/manifest.json — IMAGE-01
    - test_object_assigned_payload: OBJECT_ASSIGNED event includes image_url starting with "/static/images/" and object_name string — IMAGE-02
    - test_score_updated_payload: SCORE_UPDATED broadcast includes turn_number and scores list with player_id, player_name, turn_delta, total fields — SCORE-04
    - test_get_scores: get_scores() returns dict with player_id keys and integer total values — SCORE-05
  </behavior>
  <action>
    Create tests/test_turn_state.py following the exact structure of tests/test_turn_machine.py.

    Module-level setup:
    - Add a FakeBroadcaster class with broadcast(event_type, data, exclude=None) and send_to_player(player_id, event_type, data) methods — both append to self.events list as dicts with "type", "data" keys. send_to_player also records "player_id".
    - Each test function: one requirement ID in docstring, one assertion cluster, descriptive string in assert messages.

    All 14 test functions call pytest.skip("Wave 0 stub — implement in Plan 02/03") as the FIRST statement inside the function body (before any import of server.turn_state or server.game_server). This ensures the file can be collected and all tests appear as SKIPPED even when the production modules do not yet exist.

    DO NOT import server.turn_state or server.game_server at the module level — those imports belong inside the test functions after the skip guard is removed in Plan 02.

    Test function names must match exactly: test_submit_hint, test_submit_hint_duplicate, test_hint_received_payload, test_hint_empty_on_timer, test_all_hints_auto_advance, test_submit_guess_correct, test_skip_guess, test_guess_result_broadcast, test_guess_one_per_turn, test_guess_no_self_target, test_image_manifest_load, test_object_assigned_payload, test_score_updated_payload, test_get_scores.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_turn_state.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest tests/test_turn_state.py exits 0 with all 14 tests shown as SKIPPED (no FAILED, no ERROR)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scoring test stubs (test_scoring.py) and image bank</name>
  <files>tests/test_scoring.py, server/images/manifest.json</files>
  <read_first>
    - tests/test_turn_machine.py — pure function test pattern (no Pyro5, no daemon, direct function call)
    - .planning/phases/04-core-turn-loop/04-PATTERNS.md — _calculate_score_deltas function signature, TurnState shape for scoring tests
    - .planning/REQUIREMENTS.md — SCORE-01 (tiered: 1st=20, 2nd=15, 3rd=10, rest=max(20-(N-1)*5,5)), SCORE-02 (solo bonus +10), SCORE-03 (owner scoring tiered by N correct guessers)
    - .planning/phases/04-core-turn-loop/04-CONTEXT.md — D-12 (manifest.json format: {filename: object_name})
  </read_first>
  <behavior>
    - test_tiered_guessers: 4 players with correct_guesses in order [p1,p2,p3,p4] → deltas p1=20, p2=15, p3=10, p4=5 — SCORE-01
    - test_solo_bonus: 1 correct guesser → that player gets +10 bonus on top of position points — SCORE-02
    - test_owner_scoring: owner gets +15 if 1 guessed correctly, +10 if 2, +5 if 3, -10 if all guessers guessed correctly, 0 if none guessed — SCORE-03
    - test_score_updated_payload_shape: SCORE_UPDATED broadcast data dict has keys: room_code, turn_number, scores (list of dicts each with player_id, player_name, turn_delta, total) — SCORE-04
  </behavior>
  <action>
    Create tests/test_scoring.py with 4 test functions. All call pytest.skip("Wave 0 stub — implement in Plan 03") as first statement. DO NOT import server.turn_machine._calculate_score_deltas at module level.

    Test function names: test_tiered_guessers, test_solo_bonus, test_owner_scoring, test_score_updated_payload_shape.

    Each function body (after the skip) constructs a TurnState directly with known player_ids, image_assignments, guesses_made, and correct_guesses, then calls _calculate_score_deltas(ts) and asserts exact delta values per the REQUIREMENTS.md formulas.

    Also create server/images/ directory and server/images/manifest.json. The manifest JSON object must have at least 8 entries. Use only lowercase single-word English nouns as object_name values (these are the canonical answers for guess arbitration). Use .jpg extension for all filenames. Example entries: apple.jpg→apple, bicycle.jpg→bicycle, chair.jpg→chair, clock.jpg→clock, guitar.jpg→guitar, hat.jpg→hat, laptop.jpg→laptop, umbrella.jpg→umbrella, book.jpg→book, cup.jpg→cup, shoe.jpg→shoe, tree.jpg→tree (12 entries gives 3 turns for 4 players without repeat).

    Create placeholder image files for each manifest entry using Python's Pillow: a 100×100 RGB image with a solid color labeled with the object name. Use PIL.Image.new("RGB", (100, 100), color=(200,200,200)) and save as JPEG. This satisfies Flask static serving and IMAGE-03 without requiring real photography.

    Pillow is already in requirements.txt (Pillow==12.1.0). Generate the images programmatically via a small inline Python script run with subprocess or directly as part of the task (do not create a helper script file — generate inline).
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_scoring.py -v && python -c "import json; d=json.load(open('server/images/manifest.json')); assert len(d)>=8, f'only {len(d)} entries'; print(f'manifest OK: {len(d)} entries')"</automated>
  </verify>
  <done>pytest tests/test_scoring.py exits 0 with all 4 tests SKIPPED; server/images/manifest.json exists with ≥8 entries; ≥8 .jpg files exist in server/images/ matching manifest keys</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | Wave 0 produces no production code — no trust boundaries active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing | submit_hint / submit_guess | defer | Test stubs document the SID-map requirement; enforcement implemented in Plan 03 |
| T-4-02 | Tampering | hint_word / guess_word injection | defer | Test stub test_submit_hint_duplicate covers duplicate; length guard implemented in Plan 03 |
| T-4-03 | Tampering | self-target guess | defer | test_guess_no_self_target stub documents requirement; enforcement in Plan 03 |
| T-4-04 | Tampering | image path traversal | defer | send_from_directory protection implemented in Plan 04 (Flask route) |
</threat_model>

<verification>
Run after both tasks complete:

```
cd /home/spacko/projects/faculdade/sd-rpc-av-1 && python -m pytest tests/test_turn_state.py tests/test_scoring.py -v
```

Expected: 18 SKIPPED, 0 FAILED, exit code 0.

```
python -c "import json; d=json.load(open('server/images/manifest.json')); print(list(d.items())[:3]); assert len(d)>=8"
ls server/images/*.jpg | wc -l
```

Expected: ≥8 .jpg files in server/images/.

Full suite must not regress existing tests:
```
python -m pytest tests/ -x
```
</verification>

<success_criteria>
- pytest tests/test_turn_state.py: 14 SKIPPED, 0 FAILED
- pytest tests/test_scoring.py: 4 SKIPPED, 0 FAILED
- server/images/manifest.json: valid JSON, ≥8 entries, each value is a single lowercase English noun
- server/images/: ≥8 .jpg files whose basenames match the manifest keys
- Full suite (pytest tests/): no regressions from Phases 1–3 tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-turn-loop/04-01-SUMMARY.md`
</output>
