---
phase: 01-rpc-infrastructure-callback-pipeline
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - config.py
  - requirements.txt
  - pytest.ini
  - server/__init__.py
  - bridge/__init__.py
  - client/__init__.py
  - tests/__init__.py
  - tests/test_unit.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06

must_haves:
  truths:
    - "venv created from python3.11; importing Pyro5, flask_socketio exits 0"
    - "requirements.txt contains exactly Pyro5==5.16, Flask, flask-socketio==5.6.1, simple-websocket, pytest"
    - "config.py exports NS_HOST, GAME_SERVER_PORT=9091, BRIDGE_PORT=5000"
    - "pytest.ini defines testpaths=tests; venv/bin/pytest tests/ -x -q exits 0 with stub collection"
    - "test_unit.py contains stub functions: test_ping, test_register_callback, test_broadcast_delivery, test_per_thread_proxy"
  artifacts:
    - path: "config.py"
      provides: "Shared constants for all processes"
      contains: "NS_HOST"
    - path: "requirements.txt"
      provides: "Pinned dependency manifest"
      contains: "Pyro5==5.16"
    - path: "pytest.ini"
      provides: "pytest configuration"
      contains: "testpaths"
    - path: "tests/test_unit.py"
      provides: "Test stubs for Wave 1 + 2 implementation"
      exports: ["test_ping", "test_register_callback", "test_broadcast_delivery", "test_per_thread_proxy"]
  key_links:
    - from: "config.py"
      to: "server/game_server.py, bridge/bridge.py, client/test_client.py"
      via: "import config"
      pattern: "import config"
    - from: "venv/bin/python"
      to: "python3.11"
      via: "python3.11 -m venv venv"
      pattern: "python3.11"
---

<objective>
Bootstrap the project: create a Python 3.11 virtual environment, install all pinned dependencies, lay out the directory structure (server/, bridge/, client/, tests/ with __init__.py files), write config.py with shared constants, write pytest.ini, and write test stubs that will fail with `pytest.skip` or `NotImplementedError` until Wave 1 implements them.

Purpose: Every subsequent plan depends on the venv, directory structure, and config.py existing. This plan is the foundation — nothing else can run without it.
Output: Runnable venv, importable packages, pytest that collects 4 stub tests, config.py with NS_HOST/GAME_SERVER_PORT/BRIDGE_PORT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create venv, install dependencies, write requirements.txt</name>
  <files>requirements.txt, venv/ (created by CLI — not tracked in git)</files>
  <read_first>
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Standard Stack table, Installation section — pinned versions and exact pip install command)
    - CLAUDE.md (Version Summary table — Pyro5==5.16, flask-socketio==5.6.1, simple-websocket, Flask 3.1.x)
  </read_first>
  <action>
    Run `python3.11 -m venv venv` at the project root to create the virtual environment from Python 3.11.2 (at /usr/bin/python3.11). Do NOT use bare `python3` — it resolves to 3.8.20 via mise on this machine.

    Then run: `venv/bin/pip install Pyro5==5.16 Flask flask-socketio==5.6.1 simple-websocket pytest`

    After install, write requirements.txt at the project root with the following lines (one per line, in this order):
    - Pyro5==5.16
    - Flask
    - flask-socketio==5.6.1
    - simple-websocket
    - pytest

    Add venv/ to .gitignore (create .gitignore if absent, append `venv/` if already present).
  </action>
  <verify>
    <automated>venv/bin/python -c "import Pyro5; import flask_socketio; import flask; import simple_websocket; import pytest; print('OK')"</automated>
  </verify>
  <done>venv/bin/python imports Pyro5, flask_socketio, flask, simple_websocket, and pytest without error; requirements.txt exists with Pyro5==5.16 on line 1; .gitignore contains venv/</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure, config.py, and pytest.ini</name>
  <files>
    config.py,
    pytest.ini,
    server/__init__.py,
    bridge/__init__.py,
    client/__init__.py,
    tests/__init__.py,
    tests/test_unit.py
  </files>
  <read_first>
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md (D-06: directory layout, D-07: config.py contents, D-02: NS_HOST from env var PYRO_NS_HOST with default 127.0.0.1, D-07: GAME_SERVER_PORT=9091, BRIDGE_PORT=5000)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Recommended Project Structure section, Validation Architecture section)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-VALIDATION.md (Wave 0 Requirements, Per-Task Verification Map)
  </read_first>
  <action>
    Create the four subdirectories and their __init__.py files:
    - server/__init__.py — empty
    - bridge/__init__.py — empty
    - client/__init__.py — empty
    - tests/__init__.py — empty

    Write config.py at the project root. It must:
    - Import os at the top
    - Define NS_HOST = os.environ.get("PYRO_NS_HOST", "127.0.0.1") — per D-02; this is the single source of truth for the Name Server host
    - Define GAME_SERVER_PORT = 9091 — per D-07
    - Define BRIDGE_PORT = 5000 — per D-07
    - Define GAME_SERVER_NAME = "game.server" — the Pyro5 registration name used in both server and client

    Write pytest.ini at the project root:
    - Section: [pytest]
    - testpaths = tests
    - python_files = test_*.py
    - python_classes = Test*
    - python_functions = test_*

    Write tests/test_unit.py with four stub test functions that each call pytest.skip("not implemented yet") as their only body line. The four function names are exactly:
    - test_ping
    - test_register_callback
    - test_broadcast_delivery
    - test_per_thread_proxy

    Add a module-level docstring: "Phase 1 unit tests — real in-process Pyro5 daemon (D-04, D-05). Stubs filled in by Plans 02 and 03."
  </action>
  <verify>
    <automated>venv/bin/pytest tests/ -x -q 2>&1 | grep -E "skipped|passed|error"</automated>
  </verify>
  <done>
    - All four directories contain __init__.py
    - config.py contains NS_HOST, GAME_SERVER_PORT, BRIDGE_PORT, GAME_SERVER_NAME
    - pytest.ini contains testpaths = tests
    - `venv/bin/pytest tests/ -x -q` exits 0 with 4 skipped (no failures, no collection errors)
    - `grep -v '^#' config.py | grep -c NS_HOST` returns 1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → venv | pip install from PyPI; packages are trusted (pinned versions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt | accept | Pinned exact versions (Pyro5==5.16, flask-socketio==5.6.1) prevent supply-chain drift; dev-only machine |
| T-01-02 | Elevation of Privilege | config.py GAME_SERVER_PORT/NS_HOST | accept | All ports bind to 127.0.0.1 in subsequent plans; config.py is read-only constants, not user input |
</threat_model>

<verification>
After both tasks complete:
1. `venv/bin/python -c "import Pyro5; import flask_socketio; print('OK')"` exits 0
2. `cat requirements.txt | grep Pyro5` returns `Pyro5==5.16`
3. `venv/bin/pytest tests/ -x -q` exits 0 with 4 skipped
4. `python3.11 --version` returns `Python 3.11.2` (verify venv was built from 3.11, not 3.8)
5. `venv/bin/python --version` returns `Python 3.11.x`
</verification>

<success_criteria>
- venv built from python3.11 (not python3/3.8)
- All 5 packages import cleanly inside venv
- requirements.txt has exactly 5 lines with pinned or unversioned entries per CLAUDE.md
- config.py defines NS_HOST (from env PYRO_NS_HOST, default 127.0.0.1), GAME_SERVER_PORT=9091, BRIDGE_PORT=5000, GAME_SERVER_NAME="game.server"
- pytest collects 4 skipped tests with zero errors
- server/, bridge/, client/, tests/ all have __init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/01-rpc-infrastructure-callback-pipeline/01-01-SUMMARY.md`
</output>
