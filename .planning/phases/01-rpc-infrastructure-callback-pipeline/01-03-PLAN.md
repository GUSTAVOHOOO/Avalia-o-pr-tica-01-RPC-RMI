---
phase: 01-rpc-infrastructure-callback-pipeline
plan: "03"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - server/game_server.py
  - client/test_client.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-06

must_haves:
  truths:
    - "GameServer.broadcast_test() is decorated with @Pyro5.api.oneway — caller returns immediately without waiting for callbacks to complete"
    - "test_client.py discovers game.server via the Name Server using locate_ns(host=config.NS_HOST) — no hardcoded Pyro URI"
    - "test_client.py starts a mini Pyro5 daemon in a background thread to receive server-pushed callbacks"
    - "When run in the 3-terminal setup, test_client.py prints [PUSH RECEIVED] within 10 seconds of starting"
    - "test_client.py source contains no hardcoded IP:port for game.server — only PYRONAME:game.server lookup"
  artifacts:
    - path: "server/game_server.py"
      provides: "broadcast_test() @oneway method added to GameServer"
      contains: "@Pyro5.api.oneway"
    - path: "client/test_client.py"
      provides: "CLI smoke test — callback registration + wait for push event"
      exports: ["TestCallback", "main"]
  key_links:
    - from: "client/test_client.py"
      to: "Pyro5 Name Server"
      via: "locate_ns(host=config.NS_HOST)"
      pattern: "locate_ns"
    - from: "client/test_client.py"
      to: "GameServer.register_callback()"
      via: "proxy.register_callback('test-cli', str(cb_uri))"
      pattern: "register_callback"
    - from: "GameServer.broadcast_test()"
      to: "EventBroadcaster.broadcast()"
      via: "self.broadcaster.broadcast('test_event', ...)"
      pattern: "broadcaster.broadcast"
---

<objective>
Add broadcast_test() @oneway to GameServer and write the CLI test_client.py demo artifact. broadcast_test() is the server-side trigger that fires a push event to all registered callbacks without blocking the caller. test_client.py is the Phase 1 demo script: it discovers GameServer via the Name Server, registers a callback receiver, waits for a pushed event, prints it, and exits.

Purpose: This plan delivers the two artifacts needed for the 3-terminal smoke test (ROADMAP Phase 1 success criterion 3). Together with Plan 02's GameServer and Plan 04's bridge, this completes the callback pipeline.
Output: GameServer with @oneway broadcast_test(), and client/test_client.py that can print [PUSH RECEIVED] in a 3-terminal demo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-02-SUMMARY.md

<interfaces>
<!-- From server/game_server.py (Plan 02 output) -->
@Pyro5.api.expose
class GameServer:
    def ping(self) -> str: ...         # returns "pong"
    def register_callback(self, player_id: str, callback_uri: str) -> bool: ...

<!-- From server/event_broadcaster.py (Plan 02 output) -->
class EventBroadcaster:
    def register_callback(self, player_id: str, callback_uri: str): ...
    def broadcast(self, event_type: str, data: dict, exclude=None): ...
    def send_to_player(self, player_id: str, event_type: str, data: dict): ...

<!-- From config.py (Plan 01 output) -->
NS_HOST: str          # default "127.0.0.1"
GAME_SERVER_PORT: int  # 9091
GAME_SERVER_NAME: str  # "game.server"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add broadcast_test() @oneway to GameServer</name>
  <files>server/game_server.py</files>
  <read_first>
    - server/game_server.py (current file state — read BEFORE editing; add broadcast_test() without removing anything)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Pattern 1 code example — @Pyro5.api.oneway on broadcast_test(), Anti-Patterns: @oneway missing causes deadlock)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md (D-09: all broadcast-triggering methods must be @oneway)
  </read_first>
  <action>
    Add a new method broadcast_test(self, message: str) to the existing GameServer class. The method must have two decorators applied in this order (outer to inner): @Pyro5.api.oneway first, then the method definition. Pyro5 requires @oneway to be the innermost decorator after @expose (which is on the class). The method body calls self.broadcaster.broadcast("test_event", {"message": message, "source": "broadcast_test"}).

    Do NOT remove or change ping(), register_callback(), __init__, or the __main__ block. This is an additive change only.

    The @oneway decorator is critical: without it, a client calling broadcast_test() while a callback receiver is registered will deadlock (the server handler thread waits for the callback to complete, which waits for the server — circular). Per D-09.
  </action>
  <verify>
    <automated>venv/bin/python -c "import Pyro5.api; from server.game_server import GameServer; g = GameServer(); print(hasattr(g, 'broadcast_test'))"</automated>
  </verify>
  <done>
    - server/game_server.py contains broadcast_test method
    - `grep -c "@Pyro5.api.oneway" server/game_server.py` returns at least 1
    - `venv/bin/pytest tests/ -x -q` still exits 0 (3 passed, 1 skipped — nothing regressed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write client/test_client.py CLI smoke test</name>
  <files>client/test_client.py</files>
  <read_first>
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Pattern 5: CLI Test Client with callback daemon — full code example with TestCallback class, daemon setup, register_callback call, time.sleep(10), daemon.shutdown())
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md (D-04: manual 3-terminal procedure proves end-to-end; test_client.py is the demo artifact. "Specific Ideas" section: discovers via NS, registers callback, waits for push event, prints it, exits)
    - config.py (current file — NS_HOST, GAME_SERVER_NAME used in locate_ns and PYRONAME lookup)
  </read_first>
  <action>
    Write client/test_client.py as a standalone CLI script. It must:

    1. Import: threading, time, sys, Pyro5.api, and config (import config — not from config import ..., because config.py is at the project root and must be importable from client/).

    2. Define TestCallback class decorated with @Pyro5.api.expose. It has:
       - self.received = [] in __init__
       - on_test_event(self, data: dict) decorated with @Pyro5.api.callback and @Pyro5.api.oneway; prints "[PUSH RECEIVED] {data}" and appends data to self.received

    3. Define a main() function that:
       a. Creates Pyro5.api.Daemon(host="127.0.0.1") for the callback receiver
       b. Creates a TestCallback instance, registers it with the daemon, stores the URI
       c. Starts daemon.requestLoop in a background thread with daemon=True
       d. Uses a with-block: with Pyro5.api.Proxy(f"PYRONAME:{config.GAME_SERVER_NAME}@{config.NS_HOST}") as server:
          - Calls server.register_callback("test-cli", str(cb_uri))
          - Prints "Registered callback. Waiting 10 seconds for push events..."
          - Sleeps 10 seconds
       e. Calls daemon.shutdown()
       f. If cb.received is empty: prints "WARNING: No push events received in 10 seconds." and sys.exit(1)
       g. Prints f"Done. Received {len(cb.received)} push event(s)."

    4. if __name__ == "__main__": call main()

    CRITICAL: The PYRONAME lookup must include @{config.NS_HOST} to avoid UDP broadcast per D-01. The URI must be f"PYRONAME:{config.GAME_SERVER_NAME}@{config.NS_HOST}" — not bare PYRONAME:game.server.

    NOTE: test_client.py is a manual demo script, not a pytest test. It is not in the tests/ directory and is not imported by pytest.
  </action>
  <verify>
    <automated>venv/bin/python -c "import ast, sys; ast.parse(open('client/test_client.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - client/test_client.py parses without syntax errors
    - `grep "PYRONAME:" client/test_client.py` returns exactly one match containing "@{config.NS_HOST}" (no hardcoded IP:port for game.server)
    - `grep "register_callback" client/test_client.py` returns a match
    - `grep "on_test_event" client/test_client.py` returns a match
    - `grep "0.0.0.0" client/test_client.py` returns no match (daemon binds to 127.0.0.1)
    - `venv/bin/pytest tests/ -x -q` still exits 0 (3 passed, 1 skipped — client/ is not in testpaths)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broadcast_test() caller → GameServer | The @oneway method fires synchronously on caller return; no user input in Phase 1 |
| test_client callback daemon → network | Callback receiver daemon binds to 127.0.0.1 only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | broadcast_test() @oneway | accept | In Phase 1, no rate limiting needed; called manually by developer only |
| T-01-07 | Elevation of Privilege | test_client.py callback daemon bind | mitigate | Pyro5.api.Daemon(host="127.0.0.1") — loopback only, same as GameServer |
| T-01-08 | Information Disclosure | broadcast_test() message field | accept | Message is a dev-set string; no sensitive data in Phase 1 |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "@Pyro5.api.oneway" server/game_server.py` returns at least 1
2. `grep "PYRONAME:.*@{config.NS_HOST}" client/test_client.py` returns a match (or equivalent f-string pattern)
3. `venv/bin/python -c "import ast; ast.parse(open('client/test_client.py').read()); print('OK')"` exits 0
4. `venv/bin/pytest tests/ -x -q` exits 0 with 3 passed, 1 skipped (no regressions)
5. Manual 3-terminal check (NS + GameServer + test_client.py): test_client.py prints "Registered callback. Waiting 10 seconds..." without error (NS and GameServer must be running for this)
</verification>

<success_criteria>
- GameServer.broadcast_test() exists with @Pyro5.api.oneway, calls broadcaster.broadcast("test_event", ...)
- test_client.py is a complete, runnable CLI script that discovers game.server via NS lookup (no hardcoded URI), registers a TestCallback receiver, waits 10 seconds, exits 0 if events received
- No hardcoded game.server IP:port in test_client.py
- All previous tests still green (3 passed, 1 skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/01-rpc-infrastructure-callback-pipeline/01-03-SUMMARY.md`
</output>
