---
phase: 01-rpc-infrastructure-callback-pipeline
plan: "02"
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - server/game_server.py
  - server/event_broadcaster.py
  - tests/test_unit.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-06

must_haves:
  truths:
    - "GameServer.ping() returns 'pong' over a real in-process Pyro5 RPC call"
    - "register_callback(player_id, callback_uri) stores callback URI; a second call with the same player_id overwrites it"
    - "EventBroadcaster.broadcast() iterates all registered callback proxies and calls on_<event_type>(data) on each"
    - "GameServer is accessible via Name Server lookup under 'game.server' (unit test uses direct URI; smoke test uses NS)"
    - "All three unit tests (test_ping, test_register_callback, test_broadcast_delivery) pass with a real in-process daemon"
  artifacts:
    - path: "server/game_server.py"
      provides: "GameServer Pyro5 daemon — ping(), register_callback(), daemon startup"
      contains: "@Pyro5.api.expose"
    - path: "server/event_broadcaster.py"
      provides: "EventBroadcaster — callbacks dict, broadcast(), send_to_player()"
      contains: "class EventBroadcaster"
    - path: "tests/test_unit.py"
      provides: "Real in-process daemon tests for INFRA-01, INFRA-02, INFRA-03"
      exports: ["test_ping", "test_register_callback", "test_broadcast_delivery"]
  key_links:
    - from: "server/game_server.py"
      to: "server/event_broadcaster.py"
      via: "self.broadcaster = EventBroadcaster()"
      pattern: "EventBroadcaster"
    - from: "tests/test_unit.py"
      to: "server/game_server.py"
      via: "Pyro5.api.Proxy(uri).ping()"
      pattern: "Pyro5.api.Proxy"
---

<objective>
Implement the core RPC layer: GameServer class with @expose, ping(), and register_callback(); EventBroadcaster class with broadcast() and send_to_player(). Replace the test stubs from Plan 01 with real in-process Pyro5 daemon tests for INFRA-01, INFRA-02, and INFRA-03.

Purpose: This is the vertical slice that proves Pyro5 RPC actually works end-to-end on the target machine. test_ping, test_register_callback, and test_broadcast_delivery must go from skipped to green.
Output: server/game_server.py, server/event_broadcaster.py, and three passing pytest tests using a real Pyro5 daemon in a thread.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md
@.planning/phases/01-rpc-infrastructure-callback-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From config.py (created in Plan 01) -->
NS_HOST: str = os.environ.get("PYRO_NS_HOST", "127.0.0.1")  # default 127.0.0.1
GAME_SERVER_PORT: int = 9091
BRIDGE_PORT: int = 5000
GAME_SERVER_NAME: str = "game.server"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EventBroadcaster and GameServer core</name>
  <files>server/event_broadcaster.py, server/game_server.py</files>
  <read_first>
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Pattern 1: GameServer Daemon Startup, Pattern 2: EventBroadcaster with Lock, Anti-Patterns section — especially @oneway and shared proxy warnings)
    - PRD.md lines 798–835 (EventBroadcaster reference implementation with callbacks dict, lock, broadcast(), send_to_player())
    - PRD.md lines 407–421 (register_callback signature: player_id: str, callback_uri: str -> bool)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-CONTEXT.md (D-09: broadcast methods must be @oneway, D-06: file layout)
    - config.py (NS_HOST, GAME_SERVER_PORT, GAME_SERVER_NAME — read current file state before writing)
  </read_first>
  <behavior>
    - EventBroadcaster.__init__: self.callbacks = {} (player_id -> Proxy), self.lock = threading.Lock()
    - EventBroadcaster.register_callback(player_id, callback_uri): acquires lock, sets self.callbacks[player_id] = Pyro5.api.Proxy(callback_uri)
    - EventBroadcaster.broadcast(event_type, data, exclude=None): snapshots dict under lock, iterates snapshot, calls getattr(proxy, "on_" + event_type.lower())(data) on each, removes failed entries from self.callbacks
    - EventBroadcaster.send_to_player(player_id, event_type, data): targeted emit to one proxy; raises KeyError silently if player_id absent
    - GameServer class decorated with @Pyro5.api.expose
    - GameServer.__init__: self.lock = threading.RLock(), self.broadcaster = EventBroadcaster()
    - GameServer.ping() -> str: returns "pong"
    - GameServer.register_callback(player_id: str, callback_uri: str) -> bool: validates both args are non-empty strings (raises ValueError if not — per security context input validation); delegates to self.broadcaster.register_callback(); returns True
    - game_server.py __main__ block: creates Pyro5.api.Daemon(host="127.0.0.1", port=config.GAME_SERVER_PORT), registers GameServer under objectId=config.GAME_SERVER_NAME, calls locate_ns(host=config.NS_HOST) to register URI, prints "GameServer ready at {uri}", calls daemon.requestLoop()
    - CRITICAL: broadcast_test() is added in Plan 03, not here. Do NOT add it now — Plan 03 depends on this plan completing first.
  </behavior>
  <action>
    Write server/event_broadcaster.py: EventBroadcaster class with __init__, register_callback(player_id, callback_uri), broadcast(event_type, data, exclude=None), and send_to_player(player_id, event_type, data). Use threading.Lock for self.lock. In broadcast(), snapshot the dict under lock, iterate outside the lock, remove failed entries from self.callbacks under lock. Import threading and Pyro5.api at the top.

    Write server/game_server.py: import Pyro5.api, threading, config, and EventBroadcaster from server.event_broadcaster. Define GameServer class decorated with @Pyro5.api.expose. The __init__ creates self.lock = threading.RLock() and self.broadcaster = EventBroadcaster(). ping() returns "pong". register_callback(player_id, callback_uri) validates both args are non-empty strings (raise ValueError("player_id and callback_uri must be non-empty strings") if either is empty or not a str), delegates to self.broadcaster.register_callback(), returns True.

    The __main__ block must bind the daemon to host="127.0.0.1" (never 0.0.0.0) and port=config.GAME_SERVER_PORT, register the server under objectId=config.GAME_SERVER_NAME, call locate_ns(host=config.NS_HOST) for NS registration (per D-01/D-02 — no UDP broadcast), print the URI, then call daemon.requestLoop().
  </action>
  <verify>
    <automated>venv/bin/python -c "from server.game_server import GameServer; g = GameServer(); print(g.ping())"</automated>
  </verify>
  <done>
    - server/event_broadcaster.py contains class EventBroadcaster with register_callback, broadcast, send_to_player
    - server/game_server.py contains @Pyro5.api.expose on GameServer class, ping() returning "pong", register_callback() with non-empty validation
    - `venv/bin/python -c "from server.game_server import GameServer; g = GameServer(); print(g.ping())"` prints "pong"
    - `grep -c "@Pyro5.api.expose" server/game_server.py` returns at least 1
    - game_server.py __main__ block calls locate_ns(host=config.NS_HOST) — not locate_ns() without args
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace test stubs — implement test_ping, test_register_callback, test_broadcast_delivery</name>
  <files>tests/test_unit.py</files>
  <read_first>
    - tests/test_unit.py (current file state — read BEFORE writing; it contains 4 stubs from Plan 01)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-RESEARCH.md (Pattern: In-process daemon for unit tests, D-04/D-05 — real in-process daemon, not mocks)
    - .planning/phases/01-rpc-infrastructure-callback-pipeline/01-VALIDATION.md (Per-Task Verification Map — exact test names and requirements they cover)
  </read_first>
  <behavior>
    - test_ping: start an in-process Pyro5 daemon in a background thread (no NS needed), register GameServer, create a Proxy with the returned URI, call proxy.ping(), assert result == "pong", shutdown daemon
    - test_register_callback: start in-process daemon, register GameServer, call proxy.register_callback("p1", "PYRO:fake@127.0.0.1:9999"), assert True returned; call again with same player_id and different URI, assert True; inspect broadcaster.callbacks["p1"] to confirm overwrite; also test that register_callback("", "uri") raises Pyro5.errors.PyroError (ValueError propagated over RPC)
    - test_broadcast_delivery: start in-process daemon for GameServer; start a second in-process daemon for a TestCallbackReceiver that has on_test_event(data) storing data in a list; register GameServer; register receiver; call proxy.register_callback("r1", str(receiver_uri)); call server_obj.broadcaster.broadcast("test_event", {"msg": "hello"}) directly (not via RPC — avoids needing @oneway for this test); sleep 0.1s; assert receiver.received == [{"msg": "hello"}]
    - test_per_thread_proxy: keep as pytest.skip("implemented in Plan 04") — this test belongs to Wave 3 (bridge per-thread proxy)
  </behavior>
  <action>
    Rewrite tests/test_unit.py. Preserve the module-level docstring. Add the following imports: threading, time, pytest, Pyro5.api, Pyro5.errors, server.game_server.GameServer.

    Implement test_ping using a helper that creates a Pyro5.api.Daemon(host="127.0.0.1"), calls daemon.register(GameServer(), objectId="test.ping"), starts requestLoop in a daemon thread, creates a Proxy with the URI, calls proxy.ping(), asserts "pong", calls daemon.shutdown().

    Implement test_register_callback: same daemon pattern; call proxy.register_callback("p1", "PYRO:fake@127.0.0.1:9999") and assert True; call proxy.register_callback("p1", "PYRO:fake2@127.0.0.1:9998") and assert True. Call proxy.register_callback("", "PYRO:fake@127.0.0.1:9999") and assert that a Pyro5.errors.PyroError is raised (the ValueError from server propagates as PyroError).

    Implement test_broadcast_delivery: Create a TestCallbackReceiver class decorated with @Pyro5.api.expose; it has received = [] and on_test_event(self, data) that appends data to self.received. Start a daemon for GameServer and a separate daemon for the receiver. Get the GameServer object reference (not via proxy — create it directly and register it). Call server_obj.broadcaster.register_callback("r1", str(cb_uri)). Call server_obj.broadcaster.broadcast("test_event", {"msg": "hello"}). Sleep 0.2s. Assert len(receiver_obj.received) == 1 and receiver_obj.received[0] == {"msg": "hello"}. Shutdown both daemons.

    Leave test_per_thread_proxy as: pytest.skip("implemented in Plan 04 — bridge per-thread proxy test")
  </action>
  <verify>
    <automated>venv/bin/pytest tests/test_unit.py::test_ping tests/test_unit.py::test_register_callback tests/test_unit.py::test_broadcast_delivery -x -v</automated>
  </verify>
  <done>
    - test_ping passes (not skipped)
    - test_register_callback passes, including the ValueError-raises-PyroError assertion
    - test_broadcast_delivery passes with the receiver receiving exactly {"msg": "hello"}
    - test_per_thread_proxy is still skipped (not failing)
    - `venv/bin/pytest tests/ -x -q` exits 0 with 3 passed, 1 skipped
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC caller → GameServer.register_callback() | player_id and callback_uri arrive as untrusted strings over the Pyro5 wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | GameServer.register_callback() | mitigate | Validate both player_id and callback_uri are non-empty strings; raise ValueError("player_id and callback_uri must be non-empty strings") before storing |
| T-01-04 | Elevation of Privilege | Pyro5 daemon bind address | mitigate | Daemon binds to host="127.0.0.1" only — never 0.0.0.0; enforced in __main__ block |
| T-01-05 | Denial of Service | EventBroadcaster — failed callbacks accumulate | accept | Failed proxies removed from self.callbacks in broadcast(); low risk at 2-4 player scale |
</threat_model>

<verification>
After both tasks complete:
1. `venv/bin/pytest tests/test_unit.py::test_ping -x -v` → PASSED
2. `venv/bin/pytest tests/test_unit.py::test_register_callback -x -v` → PASSED
3. `venv/bin/pytest tests/test_unit.py::test_broadcast_delivery -x -v` → PASSED
4. `venv/bin/pytest tests/ -x -q` → 3 passed, 1 skipped, exits 0
5. `grep "host=\"127.0.0.1\"" server/game_server.py` returns a match (daemon binds to loopback only)
6. `grep "locate_ns(host=config.NS_HOST)" server/game_server.py` returns a match (no bare locate_ns())
</verification>

<success_criteria>
- server/game_server.py: @Pyro5.api.expose on GameServer, ping() returns "pong", register_callback() validates non-empty inputs, __main__ binds to 127.0.0.1 and uses locate_ns(host=config.NS_HOST)
- server/event_broadcaster.py: EventBroadcaster with lock-protected callbacks dict, broadcast() with snapshot-outside-lock pattern, failed entry removal
- 3 unit tests pass (test_ping, test_register_callback, test_broadcast_delivery) using real in-process Pyro5 daemons (not mocks)
- test_per_thread_proxy remains skipped (not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/01-rpc-infrastructure-callback-pipeline/01-02-SUMMARY.md`
</output>
