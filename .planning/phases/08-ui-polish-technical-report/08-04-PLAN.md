---
phase: 08-ui-polish-technical-report
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ScoreDeltaToast.tsx
  - frontend/src/components/ScoreDeltaToast.css
autonomous: true
requirements:
  - UI-07

must_haves:
  truths:
    - "ScoreDeltaToast renders a colored delta label (+N or -N) that animates with slideUpFade keyframe"
    - "Positive delta uses color #22c55e (green), negative uses #ef4444 (red), zero uses #6b7280 (muted)"
    - "Toast is removed from DOM via onAnimationEnd callback — no setTimeout accumulation"
    - "Multiple toasts can coexist (one per player on SCORE_UPDATED)"
    - "Font size 20px, weight 600, pointer-events none (does not block clicks)"
  artifacts:
    - path: "frontend/src/components/ScoreDeltaToast.tsx"
      provides: "Animated score delta toast component"
      contains: "onAnimationEnd"
    - path: "frontend/src/components/ScoreDeltaToast.css"
      provides: "slideUpFade animation CSS"
      contains: "score-delta-toast"
  key_links:
    - from: "frontend/src/components/ScoreDeltaToast.tsx"
      to: "frontend/src/pages/GameScreen.tsx"
      via: "onDone callback → setDeltaToasts filter"
      pattern: "onDone.*id.*string"
---

<objective>
Create the ScoreDeltaToast component for animated per-player score deltas.

Purpose: Deliver UI-07 (animated scoring panel delta) as a standalone component. On SCORE_UPDATED, GameScreen.tsx (Plan 08-05) mounts one ScoreDeltaToast per player with non-zero delta. Each toast self-removes via onAnimationEnd. Building standalone keeps Wave 1 parallel.

Output: frontend/src/components/ScoreDeltaToast.tsx + ScoreDeltaToast.css.
</objective>

<execution_context>
@/home/spacko/.claude/get-shit-done/workflows/execute-plan.md
@/home/spacko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ui-polish-technical-report/08-CONTEXT.md
@.planning/phases/08-ui-polish-technical-report/08-RESEARCH.md
@.planning/phases/08-ui-polish-technical-report/08-PATTERNS.md
@.planning/phases/08-ui-polish-technical-report/08-UI-SPEC.md

<interfaces>
<!-- ScoreDeltaToast props (source: 08-PATTERNS.md §ScoreDeltaToast.tsx): -->
```typescript
interface ScoreDeltaToastProps {
  id: string
  delta: number
  playerName: string
  onDone: (id: string) => void
}
```

<!-- Delta color contract (08-UI-SPEC.md §Color — Delta score animation): -->
// delta > 0  → '#22c55e' (success green)
// delta < 0  → '#ef4444' (destructive red)
// delta === 0 → '#6b7280' (muted — though GameScreen filters out zero-delta before mounting)

<!-- Label formatting: -->
// delta > 0 → `+${delta}` (e.g. "+20")
// delta <= 0 → `${delta}` (e.g. "-10" or "0")

<!-- onAnimationEnd pattern — do NOT use setTimeout (source: 08-RESEARCH.md §Don't Hand-Roll): -->
// onAnimationEnd={() => onDone(id)}
// This is declarative: React removes the element after the CSS animation ends. No race condition.

<!-- CSS class (source: 08-PATTERNS.md §ScoreDeltaToast.tsx CSS + GameScreen.css extension): -->
// .score-delta-toast is defined in GameScreen.css (Plan 08-02 appended it).
// ScoreDeltaToast.css adds only: aria-label and any component-specific overrides.
// The main animation class .score-delta-toast is already in GameScreen.css — import ScoreDeltaToast.css only for overrides/documentation.
// Actually: create ScoreDeltaToast.css as the canonical home for .score-delta-toast to avoid confusion.
// Reconciliation: GameScreen.css has the class too (added in Plan 08-02) — they are identical definitions.
// Resolution: Keep both; CSS class deduplication is harmless (same values).

<!-- Positioning context (source: 08-PATTERNS.md §ScoreDeltaToast.tsx — render in GameScreen): -->
// ScoreDeltaToast is positioned absolute within a position:relative scoring section.
// The parent (SCORING_PHASE section) must have position: relative for this to work.
// GameScreen.tsx (Plan 08-05) wraps the scoring section with style={{ position: 'relative' }}.

<!-- aria-label pattern: -->
// aria-label={`${playerName}: ${label} pontos`}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoreDeltaToast.tsx and ScoreDeltaToast.css</name>
  <files>
    frontend/src/components/ScoreDeltaToast.tsx
    frontend/src/components/ScoreDeltaToast.css
  </files>
  <read_first>
    - .planning/phases/08-ui-polish-technical-report/08-PATTERNS.md (§ScoreDeltaToast.tsx — full component code and CSS)
    - .planning/phases/08-ui-polish-technical-report/08-UI-SPEC.md (§Component Inventory §New: ScoreDeltaToast.tsx)
    - frontend/src/pages/GameScreen.css (verify .score-delta-toast is already appended from Plan 08-02 before creating the CSS file)
  </read_first>
  <action>
Create frontend/src/components/ScoreDeltaToast.tsx:

Import `'./ScoreDeltaToast.css'`.

Define `ScoreDeltaToastProps` interface: `{ id: string; delta: number; playerName: string; onDone: (id: string) => void }`.

Inline color derivation:
```typescript
const color = delta > 0 ? '#22c55e' : delta < 0 ? '#ef4444' : '#6b7280'
```

Inline label formatting:
```typescript
const label = delta > 0 ? `+${delta}` : `${delta}`
```

Render a single `<div>`:
- className: `"score-delta-toast"`
- style: `{ color }` — the color from above
- aria-label: `${playerName}: ${label} pontos`
- onAnimationEnd: `() => onDone(id)` — self-removal after animation completes (no setTimeout)
- Children: `{label}` — the formatted delta string

Do NOT use useEffect, useState, or any hooks. This is a pure stateless presentational component — it renders once, animates via CSS, and notifies parent via onAnimationEnd when done.

Create frontend/src/components/ScoreDeltaToast.css:

Define `.score-delta-toast`:
```css
.score-delta-toast {
  position: absolute;
  top: 8px;
  right: 16px;
  animation: slideUpFade 1.5s ease-out forwards;
  font-size: 20px;
  font-weight: 600;
  pointer-events: none;
  z-index: 10;
}
```

Note: `slideUpFade` keyframe is defined in GameScreen.css (appended by Plan 08-02). Do NOT redefine the keyframe here — just reference it in the animation property. The keyframe will be available at runtime because GameScreen.css is imported by GameScreen.tsx which is the page that renders ScoreDeltaToast.
  </action>
  <verify>
    <automated>test -f frontend/src/components/ScoreDeltaToast.tsx && test -f frontend/src/components/ScoreDeltaToast.css && grep -q "onAnimationEnd" frontend/src/components/ScoreDeltaToast.tsx && grep -q "onDone(id)" frontend/src/components/ScoreDeltaToast.tsx && grep -q "#22c55e" frontend/src/components/ScoreDeltaToast.tsx && grep -q "#ef4444" frontend/src/components/ScoreDeltaToast.tsx && grep -q "score-delta-toast" frontend/src/components/ScoreDeltaToast.css</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ScoreDeltaToast.tsx exists and exports default function ScoreDeltaToast
    - Component has no useState or useEffect (pure stateless)
    - onAnimationEnd={() => onDone(id)} is present on the rendered div
    - color is '#22c55e' for positive delta, '#ef4444' for negative, '#6b7280' for zero
    - label is `+${delta}` for positive, `${delta}` otherwise
    - aria-label includes playerName and label
    - ScoreDeltaToast.css defines .score-delta-toast with pointer-events: none and animation: slideUpFade 1.5s ease-out forwards
    - No @keyframes definition in ScoreDeltaToast.css (keyframe lives in GameScreen.css)
  </acceptance_criteria>
  <done>ScoreDeltaToast.tsx and ScoreDeltaToast.css created. Component self-removes via onAnimationEnd, supports multiple simultaneous toasts, uses correct semantic colors. Ready for GameScreen.tsx integration in Plan 08-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server score_updated → ScoreDeltaToast | delta values come from server-calculated scores; playerName is server-provided |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | playerName rendering in aria-label | mitigate | playerName is rendered as a JSX attribute string (`aria-label={...}`), not as innerHTML. React escapes special characters. No XSS surface. |
| T-08-04-02 | Denial of Service | Multiple toasts mounting without removal | mitigate | onAnimationEnd removes each toast from deltaToasts array in GameScreen (filter by id). No memory leak — animations always complete in 1.5s. |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
After task:
- `test -f frontend/src/components/ScoreDeltaToast.tsx && test -f frontend/src/components/ScoreDeltaToast.css` — both exist
- `grep -q "onAnimationEnd" frontend/src/components/ScoreDeltaToast.tsx` — self-removal present
- `python -m pytest tests/ -x -q` — backend tests still green
</verification>

<success_criteria>
- ScoreDeltaToast.tsx: stateless component with onAnimationEnd self-removal, correct color derivation (#22c55e/#ef4444/#6b7280), accessible aria-label
- ScoreDeltaToast.css: .score-delta-toast with pointer-events: none and slideUpFade animation reference
</success_criteria>

<output>
Create `.planning/phases/08-ui-polish-technical-report/08-04-SUMMARY.md` when done.
</output>
