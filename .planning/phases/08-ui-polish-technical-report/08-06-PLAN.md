---
phase: 08-ui-polish-technical-report
plan: 06
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - frontend/src/pages/Landing.tsx
  - frontend/src/pages/CreateGame.tsx
  - frontend/src/pages/JoinByCode.tsx
  - frontend/src/pages/Lobby.tsx
  - frontend/src/pages/pages.css
  - frontend/src/pages/PostGame.tsx
  - frontend/src/pages/PostGame.css
  - frontend/src/components/ChatPanel.tsx
  - frontend/src/components/ChatPanel.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-08
  - UI-10

must_haves:
  truths:
    - "Landing page shows two CTAs — 'Criar Partida' and 'Entrar em Partida' — with hover states on both"
    - "CreateGame and JoinByCode forms have consistent alignment and focus ring on inputs"
    - "Lobby shows player list with hover state on each player row"
    - "PostGame podium cards have hover lift effect; current player's row is highlighted in results table"
    - "ChatPanel label reads 'Chat' as heading and input placeholder is 'Mensagem...'"
    - "No console errors on any screen when loaded in browser"
  artifacts:
    - path: "frontend/src/pages/Landing.tsx"
      provides: "Polish: hover states on feature cards and CTAs"
    - path: "frontend/src/pages/PostGame.css"
      provides: "Podium hover + table row highlight"
      contains: "postgame__podium-card:hover"
    - path: "frontend/src/components/ChatPanel.tsx"
      provides: "Label 'Chat' heading + placeholder 'Mensagem...'"
      contains: "Mensagem..."
  key_links:
    - from: "frontend/src/pages/Lobby.tsx"
      to: "frontend/src/components/PlayerListItem.tsx"
      via: "renders player rows; hover class added"
      pattern: "PlayerListItem"
    - from: "frontend/src/components/ChatPanel.tsx"
      to: "frontend/src/components/ChatPanel.css"
      via: "CSS classes for chat panel layout"
      pattern: "chat-panel__"
---

<objective>
Polish all non-GameScreen screens and the ChatPanel component.

Purpose: Deliver UI-01, UI-02, UI-03, UI-08, UI-10 — hover states, focus rings, typography hierarchy, and radical chat/action visual separation — across Landing, CreateGame, JoinByCode, Lobby, PostGame, and ChatPanel. These are CSS-only changes plus 2 string changes in ChatPanel.

Output: 9 files with visual polish; no behavioral changes; no new components.
</objective>

<execution_context>
@/home/spacko/.claude/get-shit-done/workflows/execute-plan.md
@/home/spacko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ui-polish-technical-report/08-CONTEXT.md
@.planning/phases/08-ui-polish-technical-report/08-PATTERNS.md
@.planning/phases/08-ui-polish-technical-report/08-UI-SPEC.md

<interfaces>
<!-- Landing.tsx feature card polish (source: 08-PATTERNS.md §Landing.tsx): -->
// Feature cards (lines 54-68) — add Tailwind: `transition-opacity hover:opacity-90 cursor-default`
// Primary CTA — verify: already has `hover:opacity-90`. Add `active:opacity-80` if missing.
// Global focus ring in index.css already covers all inputs — no change needed.

<!-- Lobby.tsx player list hover (source: 08-PATTERNS.md §Lobby.tsx): -->
// PlayerListItem rows — add hover class: `hover:bg-[#0f1117] transition-colors cursor-default`
// Lobby.tsx renders PlayerListItem.tsx — add hover class there or via Lobby's className prop.

<!-- PostGame.css changes (source: 08-PATTERNS.md §PostGame.tsx): -->
```css
/* Podium card hover lift */
.postgame__podium-card {
  transition: transform 0.15s ease;
}
.postgame__podium-card:hover {
  transform: translateY(-2px);
}
/* Table row highlight for current player */
.postgame__table-row--mine td {
  background-color: #1a1d27;
}
```

<!-- PostGame.tsx table row change (source: 08-PATTERNS.md §PostGame.tsx): -->
// Add class to <tr> when pid === myPlayerId:
// `className={pid === myPlayerId ? 'postgame__table-row--mine' : ''}`

<!-- ChatPanel.tsx string changes (source: 08-PATTERNS.md §ChatPanel.tsx, 08-UI-SPEC.md Copywriting): -->
// Line 87: remove `<span className="chat-panel__label-text">Mensagem de chat</span>` OR change to heading "Chat"
// Line 96: placeholder="Mensagem de chat…" → placeholder="Mensagem..."
// Keep aria-label="Mensagem de chat" on the input (accessibility)
// "Enviar mensagem" submit button label is already correct — no change.

<!-- UI-10 separation (D-07, ChatPanel): -->
// ChatPanel already has distinct panel with its own background, label, and separate submit button.
// Polish only: ensure heading is "Chat" and placeholder "Mensagem..." to distinguish from game inputs.

<!-- CreateGame + JoinByCode alignment (source: 08-PATTERNS.md §CreateGame, 08-UI-SPEC.md §Spacing): -->
// Both screens use pages.css. Check form field gap is 16px (md token). Check button min-height 44px.
// Add hover state to primary submit buttons if not already: `hover:opacity-90` via Tailwind or CSS.
// Input hover border: `border-color: #6366f1` on hover — add to pages.css if not present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Landing, CreateGame, JoinByCode, and Lobby screens</name>
  <files>
    frontend/src/pages/Landing.tsx
    frontend/src/pages/CreateGame.tsx
    frontend/src/pages/JoinByCode.tsx
    frontend/src/pages/Lobby.tsx
    frontend/src/pages/pages.css
    frontend/src/components/PlayerListItem.tsx
  </files>
  <read_first>
    - frontend/src/pages/Landing.tsx (read before editing — identify feature card elements and CTA buttons)
    - frontend/src/pages/CreateGame.tsx (read before editing — identify form structure and current CSS classes)
    - frontend/src/pages/JoinByCode.tsx (read before editing — should mirror CreateGame)
    - frontend/src/pages/Lobby.tsx (read before editing — identify player list rendering and PlayerListItem usage)
    - frontend/src/components/PlayerListItem.tsx (read before editing — identify className prop or static class)
    - frontend/src/pages/pages.css (read before editing — understand existing shared form styles)
    - .planning/phases/08-ui-polish-technical-report/08-PATTERNS.md (§Landing.tsx, §Lobby.tsx polish sections)
    - .planning/phases/08-ui-polish-technical-report/08-UI-SPEC.md (§Phase-Specific Interaction States — all 5 states required)
  </read_first>
  <action>
Read all target files before editing. Apply only the changes listed below — do NOT restructure components or change layouts.

Landing.tsx changes:
- Feature cards: add Tailwind classes `transition-opacity hover:opacity-90 cursor-default` to the feature card container elements. If the cards use a static className string, add these to the end of the string.
- CTA "Criar Partida" button: verify it has `hover:opacity-90`. If not, add it. Also add `active:scale-[0.99] transition-transform`.
- CTA "Entrar em Partida" form area: ensure the join input has focus ring covered by global `*:focus-visible` rule in index.css (no change needed unless inline style overrides it — check and remove any `outline: none` that would suppress the global rule).

CreateGame.tsx and JoinByCode.tsx changes:
- Both screens share pages.css. Verify primary submit button has `hover:opacity-90` class or CSS hover rule. If using inline styles, convert to use the pages.css `page-submit-btn` class.
- Verify input fields have no `outline: none` style that would suppress the global focus ring.
- Verify form fields have 16px gap (md spacing token from UI-SPEC).

pages.css changes:
- Add hover state rule if not present: `.page-submit-btn:hover:not(:disabled) { opacity: 0.9; }`
- Add input hover border: `.page-field__input:hover:not(:disabled) { border-color: #6366f1; }` (matches accent color hover pattern from UI-SPEC §Phase-Specific Interaction States rule 2).
- These are additive only — do not remove existing rules.

Lobby.tsx changes:
- Identify how player list items are rendered. If via PlayerListItem component, add a hover class to the component wrapper.
- If the player rows are rendered inline in Lobby.tsx, add `hover:bg-[#0f1117] transition-colors` Tailwind classes to each player row element.

PlayerListItem.tsx changes:
- If the component renders player rows with a fixed className, append ` transition-colors hover:bg-[#0f1117] cursor-default` to the existing className.
- If the component uses dynamic className based on props, add these to the base class.
- No structural changes — only CSS class additions.
  </action>
  <verify>
    <automated>grep -q "hover:opacity-90" frontend/src/pages/Landing.tsx && grep -q "hover" frontend/src/components/PlayerListItem.tsx && grep -q "page-submit-btn:hover" frontend/src/pages/pages.css</automated>
  </verify>
  <acceptance_criteria>
    - Landing.tsx feature card elements have `hover:opacity-90` or equivalent hover class
    - Landing.tsx primary CTA has `hover:opacity-90`
    - pages.css contains `.page-submit-btn:hover:not(:disabled) { opacity: 0.9; }`
    - PlayerListItem.tsx contains a Tailwind `hover:` class for background change on hover
    - CreateGame.tsx and JoinByCode.tsx inputs do not have `outline: none` suppressing focus ring
    - No component structure or layout changes — only CSS class additions
  </acceptance_criteria>
  <done>Landing, CreateGame, JoinByCode, Lobby screens polished with hover states and focus rings per UI-SPEC §Phase-Specific Interaction States.</done>
</task>

<task type="auto">
  <name>Task 2: Polish PostGame screen and ChatPanel copy (UI-08, UI-10)</name>
  <files>
    frontend/src/pages/PostGame.tsx
    frontend/src/pages/PostGame.css
    frontend/src/components/ChatPanel.tsx
    frontend/src/components/ChatPanel.css
  </files>
  <read_first>
    - frontend/src/pages/PostGame.tsx (read before editing — identify podium card elements, table row rendering, myPlayerId usage)
    - frontend/src/pages/PostGame.css (read before editing — understand current podium and table styles)
    - frontend/src/components/ChatPanel.tsx (read before editing — identify exact line for label and placeholder)
    - frontend/src/components/ChatPanel.css (read before editing — understand current layout before adding anything)
    - .planning/phases/08-ui-polish-technical-report/08-PATTERNS.md (§PostGame.tsx and §ChatPanel.tsx sections)
    - .planning/phases/08-ui-polish-technical-report/08-UI-SPEC.md (§Copywriting Contract — exact strings)
  </read_first>
  <action>
Read all 4 files before editing.

PostGame.css changes (append to end of file — do NOT modify existing rules):
```css
/* Phase 8 polish */
.postgame__podium-card {
  transition: transform 0.15s ease;
}
.postgame__podium-card:hover {
  transform: translateY(-2px);
}
.postgame__table-row--mine td {
  background-color: #1a1d27;
}
```

PostGame.tsx changes:
- Find the table body `<tr>` elements that render per-player rows in the results table. Add `className` attribute: `className={pid === myPlayerId ? 'postgame__table-row--mine' : ''}`. The `myPlayerId` variable is already in scope (read the file to confirm its exact name).
- Do NOT change any other logic or layout.

ChatPanel.tsx changes (UI-10, D from CONTEXT):
- Find the label/heading element for the chat section. Per 08-PATTERNS.md §ChatPanel.tsx: if there is a `<span className="chat-panel__label-text">Mensagem de chat</span>` element, either (a) change its text to "Chat" and keep the span, or (b) replace with a heading `<h3>` with text "Chat". Use whichever approach is already closest to the existing markup.
- Find the `<input>` or `<textarea>` for chat message entry. Change its `placeholder` attribute value from "Mensagem de chat…" (or similar) to exactly: `"Mensagem..."` (per UI-SPEC Copywriting Contract).
- Verify `aria-label` on the input remains `"Mensagem de chat"` (accessibility label — different from placeholder).
- Verify "Enviar mensagem" submit button text is already correct — do not change it.

ChatPanel.css changes (append only if needed):
- If the chat label/heading needs visual distinction after the text change (e.g., font-weight update for the "Chat" heading), add: `.chat-panel__label-text { font-weight: 600; }` — but only if not already present.
  </action>
  <verify>
    <automated>grep -q "postgame__podium-card:hover" frontend/src/pages/PostGame.css && grep -q "postgame__table-row--mine" frontend/src/pages/PostGame.css && grep -q "postgame__table-row--mine" frontend/src/pages/PostGame.tsx && grep -q "Mensagem\.\.\." frontend/src/components/ChatPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PostGame.css contains `.postgame__podium-card:hover { transform: translateY(-2px); }`
    - PostGame.css contains `.postgame__table-row--mine td { background-color: #1a1d27; }`
    - PostGame.tsx table rows for current player have className `postgame__table-row--mine`
    - ChatPanel.tsx input/textarea placeholder is exactly `"Mensagem..."` (not "Mensagem de chat…")
    - ChatPanel.tsx submit button text is "Enviar mensagem" (unchanged)
    - ChatPanel.tsx input has aria-label="Mensagem de chat" (accessibility — unchanged)
    - No layout or behavioral changes in PostGame or ChatPanel
  </acceptance_criteria>
  <done>PostGame screen polished with podium hover and player row highlight. ChatPanel label updated to "Chat" heading and placeholder to "Mensagem..." per UI-SPEC copywriting contract (UI-10). All changes are additive CSS + string replacements only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → player names in PostGame | Player names come from server; rendered in table cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Player name XSS in PostGame table | mitigate | Player names rendered as JSX text nodes `{playerName}` — React escapes HTML entities. No dangerouslySetInnerHTML. |
| T-08-06-02 | Tampering | Chat message XSS in ChatPanel | mitigate | Chat messages already rendered via JSX in Phase 7 implementation. No dangerouslySetInnerHTML per security context requirement. |
| T-08-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages. CSS and string changes only. |
</threat_model>

<verification>
After both tasks:
- `grep -q "hover" frontend/src/pages/Landing.tsx` — hover states present
- `grep -q "postgame__podium-card:hover" frontend/src/pages/PostGame.css` — podium hover present
- `grep -q "Mensagem\.\.\." frontend/src/components/ChatPanel.tsx` — placeholder updated
- `python -m pytest tests/ -x -q` — backend tests still green
</verification>

<success_criteria>
- Landing: feature cards and CTAs have hover states (opacity transition)
- CreateGame + JoinByCode: submit buttons have hover state; inputs respect global focus ring
- Lobby: player list items have hover background change
- PostGame: podium cards lift on hover; current player's row is highlighted in results table
- ChatPanel: label is "Chat", placeholder is "Mensagem...", submit button unchanged, aria-label preserved
- No structural/behavioral changes in any screen
</success_criteria>

<output>
Create `.planning/phases/08-ui-polish-technical-report/08-06-SUMMARY.md` when done.
</output>
