---
phase: 07-reconnection-end-of-game
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_event_broadcaster.py
  - tests/test_reconnect.py
  - tests/test_postgame.py
  - tests/test_chat.py
  - tests/test_session.py
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
  - SESSION-07
  - POSTGAME-01
  - POSTGAME-02
  - POSTGAME-03
  - POSTGAME-04
  - CHAT-01
  - CHAT-02

must_haves:
  truths:
    - "pytest -x -q runs without collection errors"
    - "All new stubs are skipped (not errored) and clearly named per requirement ID"
    - "Wave 1 plans have named test targets to fill in"
  artifacts:
    - path: "tests/test_event_broadcaster.py"
      provides: "Stubs for INFRA-07 failure counter + PLAYER_LEFT broadcast tests"
    - path: "tests/test_reconnect.py"
      provides: "Stubs for INFRA-08 reconnect_player RPC tests"
    - path: "tests/test_postgame.py"
      provides: "Stubs for POSTGAME-01 to 04 (per-turn history, vote logic, restart, end)"
    - path: "tests/test_chat.py"
      provides: "Stubs for CHAT-01 to 02 (send_chat, broadcast payload)"
    - path: "tests/test_session.py"
      provides: "Stub for SESSION-07 host transfer on leave_game"
  key_links: []
---

<objective>
Wave 0: create test scaffold files for all Phase 7 server-side behaviors. Each file contains pytest.skip stubs naming the exact test function each Wave 1 plan will implement. Running the full suite after this plan produces only SKIPPED entries — no errors and no failures.

Purpose: Nyquist compliance requires every automated verification to have a target test before implementation begins. Wave 1 plans (07-02, 07-03) inherit these stubs and replace pytest.skip with real assertions.

Output: 4 new test files under tests/ + 1 stub appended to the existing tests/test_session.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-reconnection-end-of-game/07-CONTEXT.md
@.planning/phases/07-reconnection-end-of-game/07-RESEARCH.md
@.planning/phases/07-reconnection-end-of-game/07-VALIDATION.md
@tests/test_unit.py
@tests/test_session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_event_broadcaster.py and test_reconnect.py stubs; append SESSION-07 stub to test_session.py</name>
  <files>tests/test_event_broadcaster.py, tests/test_reconnect.py, tests/test_session.py</files>
  <read_first>
    - tests/test_unit.py (stub pattern: pytest.skip inside test function body, _start_daemon helper)
    - tests/test_session.py (pattern: GameServer in-process + Pyro5 daemon; append new stub at end of file)
    - server/event_broadcaster.py (current broadcast() — no failure_counts yet; will be added by plan 02)
    - server/game_server.py lines 48–77 (GameSession fields, no reconnect_player yet)
  </read_first>
  <action>
    Create tests/test_event_broadcaster.py with the following stubs (each calls pytest.skip with a one-line rationale):

    - test_consecutive_failure_counter: After 3 consecutive transient exceptions on broadcast(), the player_id appears in the returned failed list. Validates D-08 / INFRA-07 threshold logic.
    - test_failure_resets_on_success: After a failure followed by a successful delivery, failure_counts[player_id] is not present (reset on success). Validates D-08 counter reset.
    - test_player_left_broadcast_on_failure: GameServer._remove_failed_players() is called with the failed list; a PLAYER_LEFT event is broadcast with payload {player_id, player_name, room_code}. Validates INFRA-07 broadcast.

    Create tests/test_reconnect.py with the following stubs:

    - test_reconnect_player_returns_player_view: reconnect_player(player_id, room_code, cb_uri) on a server with an active session returns a dict containing keys "phase", "players", "room_code" (same shape as get_player_view). Validates INFRA-08 D-03.
    - test_reconnect_player_unknown_uuid: reconnect_player("invalid-uuid", "ROOM01", "PYRO:x@127.0.0.1:0") returns {"error": ...}. Validates server-side UUID validation.
    - test_reconnect_player_updates_callback_uri: After reconnect_player(), broadcaster.callbacks[player_id] equals the new callback_uri. Validates D-02 callback re-registration.

    Both new files must import pytest and use the _start_daemon helper pattern from test_session.py.

    Append to tests/test_session.py (do NOT replace existing tests — add after the last existing test function):

    - test_host_transfer_on_leave: Create a 2-player session (Player A as host, Player B as non-host). Call leave_game(player_A_id). Assert that session.host_id == player_B_id (host transferred to the remaining player). Assert that a HOST_CHANGED broadcast was received by a registered callback with keys {new_host_id, room_code}. Validates SESSION-07 host-transfer-on-leave. Use pytest.skip("stub — implemented by plan 07-02") in the body.

    The docstring at the top of tests/test_session.py currently lists SESSION-01 through SESSION-06. Append "test_host_transfer_on_leave — SESSION-07: leave_game() transfers host to next player and broadcasts HOST_CHANGED" to the coverage list in the module docstring.
  </action>
  <verify>
    <automated>python -m pytest tests/test_event_broadcaster.py tests/test_reconnect.py tests/test_session.py::test_host_transfer_on_leave -v 2>&1 | grep -E "PASSED|FAILED|ERROR|SKIPPED"</automated>
  </verify>
  <done>All 7 stubs (6 new + test_host_transfer_on_leave) appear as SKIPPED. Zero FAILED or ERROR entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_postgame.py and test_chat.py stubs</name>
  <files>tests/test_postgame.py, tests/test_chat.py</files>
  <read_first>
    - tests/test_unit.py (in-process daemon pattern)
    - tests/test_session.py (_start_daemon helper, GameServer setup)
    - .planning/phases/07-reconnection-end-of-game/07-RESEARCH.md §Phase Requirements (POSTGAME-01 to 04, CHAT-01/02)
    - .planning/phases/07-reconnection-end-of-game/07-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Create tests/test_postgame.py with the following stubs:

    - test_turn_score_history_appended: After one turn's scoring phase, session.turn_score_history has one entry shaped {"turn": 1, "scores": {player_id: int}}. Validates POSTGAME-01 data availability for the podium table.
    - test_vote_started_broadcast: _start_vote() broadcasts a "vote_started" event with keys {"room_code", "duration_seconds", "player_count"}. Validates POSTGAME-02.
    - test_vote_majority_yes_restarts: With 2 players both submitting continue_game=True, _resolve_vote() broadcasts "game_restarting" and session.vote_record is None. Validates POSTGAME-03.
    - test_vote_no_majority_ends_game: With 2 players where only 1 votes True (minority), _resolve_vote() broadcasts "game_ended" with keys {"final_scores", "turn_score_history"}. Validates POSTGAME-04.
    - test_vote_timer_expiry_ends_game: A VoteRecord with 30s timer that fires without any votes results in "game_ended" broadcast. Validates POSTGAME-04 (timer path).
    - test_duplicate_vote_ignored: A second submit_vote() from the same player_id returns {"ok": True, "duplicate": True} and does not change yes_count. Validates vote stuffing guard.

    Create tests/test_chat.py with the following stubs:

    - test_send_chat_returns_ok: send_chat(player_id, "hello") for a player in a valid session returns {"ok": True}. Validates CHAT-01.
    - test_send_chat_broadcasts_payload: broadcast() is called once with event_type "chat_message" and data containing keys {"player_id", "player_name", "message", "timestamp", "room_code"}. Validates CHAT-02.
    - test_send_chat_message_length_cap: A message of 300 characters is truncated to 200 in the broadcast payload. Validates CHAT-01 length cap.
    - test_send_chat_unknown_player: send_chat("unknown-id", "msg") returns {"error": ...}. Validates CHAT-01 player validation.

    Both files must import pytest. Use pytest.skip("stub — implemented by plan 07-02") in every test body.
  </action>
  <verify>
    <automated>python -m pytest tests/test_postgame.py tests/test_chat.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR|SKIPPED"</automated>
  </verify>
  <done>All 10 stubs appear as SKIPPED. Full suite runs without ERROR entries: python -m pytest tests/ -x -q passes (only skips, no failures).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → server | Test files call server methods directly in-process; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; test files use only stdlib + pytest (already in requirements.txt) |
</threat_model>

<verification>
- All four new test files exist under tests/
- tests/test_session.py contains test_host_transfer_on_leave as a SKIPPED stub
- python -m pytest tests/ -x -q exits 0 with only SKIPPED status for new stubs, all prior tests still pass
</verification>

<success_criteria>
- tests/test_event_broadcaster.py: 3 SKIPPED stubs
- tests/test_reconnect.py: 3 SKIPPED stubs
- tests/test_session.py: test_host_transfer_on_leave 1 SKIPPED stub (appended)
- tests/test_postgame.py: 6 SKIPPED stubs
- tests/test_chat.py: 4 SKIPPED stubs
- Full suite: python -m pytest tests/ -x -q exits 0
</success_criteria>

<output>
Create `.planning/phases/07-reconnection-end-of-game/07-01-SUMMARY.md` when done
</output>
