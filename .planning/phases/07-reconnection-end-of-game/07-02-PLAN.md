---
phase: 07-reconnection-end-of-game
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - server/event_broadcaster.py
  - server/game_server.py
  - tests/test_event_broadcaster.py
  - tests/test_reconnect.py
  - tests/test_postgame.py
  - tests/test_chat.py
  - tests/test_session.py
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
  - SESSION-07
  - POSTGAME-01
  - POSTGAME-02
  - POSTGAME-03
  - POSTGAME-04
  - CHAT-01
  - CHAT-02

must_haves:
  truths:
    - "3 consecutive transient callback failures remove a player and return their player_id in the failed list"
    - "reconnect_player() validates UUID, updates callback URI, and returns get_player_view() payload"
    - "send_chat() broadcasts a dict with player_id, player_name, message (≤200 chars), timestamp, room_code"
    - "submit_vote() tallies correctly; majority yes → GAME_RESTARTING; otherwise → GAME_ENDED after timer"
    - "turn_score_history grows by one entry per scoring phase"
    - "All 17 stubs in test_event_broadcaster.py, test_reconnect.py, test_session.py, test_postgame.py, test_chat.py are GREEN"
  artifacts:
    - path: "server/event_broadcaster.py"
      provides: "failure_counts dict, 3-failure threshold, broadcast() returns failed list"
      contains: "failure_counts"
    - path: "server/game_server.py"
      provides: "VoteRecord dataclass, GameSession.turn_score_history, reconnect_player, send_chat, _start_vote, submit_vote, _resolve_vote, _remove_failed_players, _accumulate_scores appends turn_score_history"
      contains: "def reconnect_player"
  key_links:
    - from: "server/event_broadcaster.py:broadcast()"
      to: "server/game_server.py:_remove_failed_players()"
      via: "broadcast() returns failed list; ALL callers capture return value and call _remove_failed_players() after lock exits"
      pattern: "failed.*_remove_failed_players"
    - from: "server/game_server.py:_start_vote()"
      to: "server/game_server.py:_resolve_vote()"
      via: "threading.Timer(30, _resolve_vote) with generation counter"
      pattern: "threading.Timer.*_resolve_vote"
---

<objective>
Server-side vertical slice: implement all new game_server.py methods and EventBroadcaster extensions needed by Phase 7, then fill in the Wave 0 test stubs so the full test suite is green.

Purpose: All bridge and frontend plans depend on these RPC methods existing. Implementing server + tests together in one plan proves behavior before the bridge wires up.

Output: event_broadcaster.py with failure_counts; game_server.py with reconnect_player, send_chat, VoteRecord, _start_vote, submit_vote, _resolve_vote, _remove_failed_players, GameSession.turn_score_history; all 17 Phase 7 test stubs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-reconnection-end-of-game/07-CONTEXT.md
@.planning/phases/07-reconnection-end-of-game/07-RESEARCH.md
@.planning/phases/07-reconnection-end-of-game/07-PATTERNS.md
@.planning/phases/07-reconnection-end-of-game/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor must respect. Extracted from codebase. -->

From server/event_broadcaster.py (current — to be extended):
  class EventBroadcaster:
      def __init__(self):
          self.callbacks = {}          # player_id -> uri str
          self.lock = threading.Lock()
      def register_callback(self, player_id: str, callback_uri: str)
      def unregister_callback(self, player_id: str)
      def broadcast(self, event_type: str, data: dict, exclude=None)
          # Returns None today — MUST return list[str] (failed player_ids) after this plan
      def send_to_player(self, player_id: str, event_type: str, data: dict)

From server/game_server.py (current — to be extended):
  @dataclasses.dataclass
  class GameSession:
      room_code: str
      host_id: str
      max_turns: int
      status: str  # "WAITING" | "IN_PROGRESS" | "ENDED"
      players: List[PlayerInfo]
      turn_machine: object = None
      accumulated_scores: dict = {}
      current_image_assignments: dict = {}
      # ADD: turn_score_history: list = []
      # ADD: vote_record: object = None

  class GameServer:
      self.lock: threading.RLock
      self.sessions: dict  # room_code -> GameSession
      self._player_to_room: dict  # player_id -> room_code
      self.broadcaster: EventBroadcaster
      def get_player_view(self, room_code: str, player_id: str) -> dict
          # Returns: {"phase", "remaining_seconds", "current_turn", "max_turns",
          #           "players", "object_assignment", "status", "room_code", "scores"}
      def _accumulate_scores(self, room_code: str, turn_state) -> None
          # ADD: append {"turn": turn_number, "scores": {pid: delta}} to session.turn_score_history
      def leave_game(self, player_id: str)
          # Already handles host transfer + HOST_CHANGED — SESSION-07 already implemented

Threading patterns (MANDATORY — from prior phases):
  - All new GameServer methods: acquire self.lock, snapshot broadcast payload, EXIT lock, THEN call broadcaster.broadcast()
  - broadcast() AFTER self.lock exits — never inside a lock block
  - threading.Timer(N, fn): set t.daemon = True before t.start()
  - Return {"ok": True, ...} on success; {"error": "reason"} on failure from @expose methods
  - Never raise exceptions from RPC methods
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EventBroadcaster — failure_counts + broadcast returns failed list</name>
  <files>server/event_broadcaster.py, tests/test_event_broadcaster.py</files>
  <read_first>
    - server/event_broadcaster.py (full file — current broadcast() structure, exception handling branches at lines 71–84)
    - .planning/phases/07-reconnection-end-of-game/07-PATTERNS.md §event_broadcaster.py (Pattern 3 code excerpt)
    - .planning/phases/07-reconnection-end-of-game/07-RESEARCH.md §Pattern 3: Consecutive Callback Failure Counter
    - tests/test_event_broadcaster.py (stubs created by plan 07-01)
  </read_first>
  <behavior>
    - test_consecutive_failure_counter: Mock a callback URI that always raises Exception("transient"). Call broadcast() 3 times. The 3rd call returns a list containing the player_id. The 1st and 2nd calls return an empty list (player not yet removed).
    - test_failure_resets_on_success: After 2 transient failures, a successful delivery resets failure_counts so the player_id is NOT in the returned failed list on the next failure.
    - test_player_left_broadcast_on_failure: When broadcast() returns a non-empty failed list, GameServer._remove_failed_players(failed) removes the player from session.players and broadcasts "player_left" with keys {player_id, player_name, room_code}.
  </behavior>
  <action>
    Modify server/event_broadcaster.py:

    1. In __init__: add self.failure_counts: dict = {} after self.lock assignment.

    2. In broadcast(), transient-failure branch (currently just logs): increment self.failure_counts[player_id] by 1 (default 0); if count reaches 3, append player_id to failed list (same list used for permanent failures). Keep existing permanent-failure branch unchanged.

    3. In broadcast(), successful delivery branch (inside the try after method() call succeeds): call self.failure_counts.pop(player_id, None) to reset the consecutive counter.

    4. At the end of broadcast() after the failed cleanup block: add return failed (currently returns None implicitly). This is a breaking change — all callers of broadcast() must handle the return value. Check game_server.py callers and update them to call self._remove_failed_players(failed) where failed is truthy.

    CRITICAL: failure_counts must NOT be protected by self.lock — it is iterated outside the lock (same as the rest of broadcast()). The iteration over snapshot is already outside the lock.

    Then fill in tests/test_event_broadcaster.py stubs with real assertions using mock callback URIs (register_callback with a URI pointing to a fake Pyro5 object or by patching the proxy creation). Use the in-process Pyro5 daemon approach from test_unit.py if testing real delivery; use unittest.mock.patch for testing failure counting without requiring a live daemon.
  </action>
  <verify>
    <automated>python -m pytest tests/test_event_broadcaster.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>3 tests PASSED in test_event_broadcaster.py. broadcast() returns a list (possibly empty). self.failure_counts exists on EventBroadcaster instances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Add server-side Phase 7 methods (data model, failure removal, reconnect, chat)</name>
  <files>server/game_server.py, tests/test_session.py, tests/test_reconnect.py, tests/test_chat.py</files>
  <read_first>
    - server/game_server.py lines 48–80 (GameSession dataclass — add fields here)
    - server/game_server.py lines 415–448 (_accumulate_scores — add turn_score_history append here)
    - server/game_server.py lines 450–508 (start_game — TurnMachine creation pattern, broadcast outside lock)
    - server/game_server.py lines 897–952 (leave_game — broadcast outside lock pattern + host transfer)
    - server/game_server.py lines 321–357 (get_player_view — returned shape)
    - .planning/phases/07-reconnection-end-of-game/07-PATTERNS.md §game_server.py (all code excerpts)
    - .planning/phases/07-reconnection-end-of-game/07-RESEARCH.md §Pattern 4 and Pattern 6
    - tests/test_session.py (test_host_transfer_on_leave stub from plan 07-01)
    - tests/test_reconnect.py (stubs from plan 07-01)
    - tests/test_chat.py (stubs from plan 07-01)
  </read_first>
  <behavior>
    From test_session.py:
    - test_host_transfer_on_leave: leave_game(player_A_id) with Player A as host transfers session.host_id to player_B_id; a HOST_CHANGED broadcast with keys {new_host_id, room_code} is received by the registered callback.

    From test_reconnect.py:
    - test_reconnect_player_returns_player_view: reconnect_player() for a known player_id returns dict with keys "players", "room_code"; no "error" key.
    - test_reconnect_player_unknown_uuid: reconnect_player() for unknown player_id returns {"error": ...}.
    - test_reconnect_player_updates_callback_uri: After reconnect_player(player_id, room_code, new_uri), broadcaster.callbacks[player_id] == new_uri.

    From test_chat.py:
    - test_send_chat_returns_ok: send_chat(player_id, "hello") returns {"ok": True}.
    - test_send_chat_broadcasts_payload: broadcaster received broadcast("chat_message", data) where data has all 5 keys.
    - test_send_chat_message_length_cap: 300-char message → broadcast payload message field has len 200.
    - test_send_chat_unknown_player: send_chat("unknown", "msg") returns {"error": ...}.
  </behavior>
  <action>
    Implement in server/game_server.py:

    1. Add VoteRecord dataclass near GameSession (import dataclasses already present):
       VoteRecord with fields: votes (dict, default_factory=dict), generation (int, default=0), timer (object, default=None, repr=False).

    2. Extend GameSession dataclass with two new fields after accumulated_scores:
       - turn_score_history: list = dataclasses.field(default_factory=list)
       - vote_record: object = dataclasses.field(default=None, repr=False)

    3. Modify _accumulate_scores(): after updating session.accumulated_scores, append {"turn": turn_state.turn_number, "scores": dict(deltas)} to session.turn_score_history (still inside the lock, before the broadcast).

    4. Update ALL callers of self.broadcaster.broadcast() to handle the returned failed list: run grep to find every broadcast() callsite in game_server.py, then assign `failed = self.broadcaster.broadcast(...)` for each and call `self._remove_failed_players(failed)` if failed is truthy AFTER the lock exits. New methods added in this task must also follow this pattern. There must be zero bare `self.broadcaster.broadcast(...)` calls where the return value is discarded.

    5. Add _remove_failed_players(self, failed_player_ids: list) method (per D-08 / INFRA-07):
       For each player_id in failed_player_ids: under self.lock, pop from _player_to_room, remove from session.players, call broadcaster.unregister_callback. Snapshot player_name and room_code under lock. After lock: broadcast "player_left" with {player_id, player_name, room_code}. Follow broadcast-outside-lock pattern strictly — the broadcast call must be AFTER the with block exits.

    6. Add reconnect_player(self, player_id: str, room_code: str, callback_uri: str) -> dict decorated with @Pyro5.api.expose:
       Under self.lock: verify self._player_to_room.get(player_id) == room_code; if not, return {"error": "player not in session"}. Get session. Update p.callback_uri for the matching PlayerInfo. Exit lock. Call self.broadcaster.register_callback(player_id, callback_uri). Return self.get_player_view(room_code, player_id).

    7. Add send_chat(self, player_id: str, message: str) -> dict decorated with @Pyro5.api.expose:
       Under self.lock: look up room_code and player_name from _player_to_room + session.players. Exit lock. If not found, return {"error": "player not in session"}. Call self.broadcaster.broadcast("chat_message", {player_id, player_name, message: str(message)[:200], timestamp: time.time(), room_code}). Handle failed list from broadcast(). Return {"ok": True}.

    Then fill in test_host_transfer_on_leave in tests/test_session.py with real pytest assertions (the SESSION-07 host-transfer is already implemented in leave_game(); this test confirms it). Fill in all 7 stubs in tests/test_reconnect.py and tests/test_chat.py with real assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_session.py::test_host_transfer_on_leave tests/test_reconnect.py tests/test_chat.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_host_transfer_on_leave PASSED. All 7 chat + reconnect stubs PASSED.

    Broadcast capture check — zero lines where broadcast() return is discarded:
    grep -n "broadcaster\.broadcast" server/game_server.py | grep -v "_remove_failed_players"
    (should return 0 matching lines — every broadcast() call's return value is passed to _remove_failed_players)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Add vote state + lifecycle wiring + vote tests</name>
  <files>server/game_server.py, tests/test_postgame.py</files>
  <read_first>
    - server/game_server.py (current state after Task 2a — VoteRecord and GameSession fields now exist)
    - server/turn_machine.py lines 86–104 (VoteRecord-like pattern: dataclass with timer + generation)
    - server/turn_machine.py lines 133–195 (generation-counter stale-timer guard)
    - .planning/phases/07-reconnection-end-of-game/07-PATTERNS.md §game_server.py vote patterns
    - .planning/phases/07-reconnection-end-of-game/07-RESEARCH.md §Pattern 6 (vote lifecycle)
    - tests/test_postgame.py (stubs from plan 07-01)
  </read_first>
  <behavior>
    From test_postgame.py:
    - test_turn_score_history_appended: After _accumulate_scores() is called once, session.turn_score_history[0] has keys "turn" (int) and "scores" (dict mapping player_id to int delta).
    - test_vote_started_broadcast: After _start_vote(room_code), broadcaster received one broadcast("vote_started", {room_code, duration_seconds:30, player_count:int}).
    - test_vote_majority_yes_restarts: submit_vote() for both players with continue_game=True triggers _resolve_vote() which broadcasts "game_restarting"; session.vote_record is None afterward.
    - test_vote_no_majority_ends_game: 1 of 2 players votes True → no majority → broadcasts "game_ended" with final_scores and turn_score_history keys.
    - test_vote_timer_expiry_ends_game: _start_vote() starts a timer; calling _resolve_vote(room_code, generation=1) with no votes cast broadcasts "game_ended".
    - test_duplicate_vote_ignored: Second submit_vote() from same player returns {"ok": True, "duplicate": True}.
  </behavior>
  <action>
    Add to server/game_server.py (VoteRecord and GameSession fields were added in Task 2a):

    1. Add _start_vote(self, room_code: str) internal method:
       Under self.lock: get session; return if session is None or vote_record is not None (already running). Create VoteRecord(generation=1). Set session.vote_record = vote. Snapshot player_count and gen. Build broadcast_data. Exit lock. Create threading.Timer(30.0, lambda: self._resolve_vote(room_code, gen)); set t.daemon = True. Under self.lock again: set session.vote_record.timer = t if generation still matches. Start timer. Call self.broadcaster.broadcast("vote_started", broadcast_data). Handle failed list via _remove_failed_players.

    2. Add submit_vote(self, player_id: str, continue_game: bool) -> dict decorated with @Pyro5.api.expose:
       Under self.lock: get room_code, session, vote_record. If vote_record is None return {"error": "no vote in progress"}. If player_id already in vote.votes return {"ok": True, "duplicate": True}. Record vote. Check if all players voted; if yes, set resolve_now=True with snapshots. Build vote_update broadcast_data. Exit lock. Call broadcaster.broadcast("vote_update", data). Handle failed. If resolve_now: call _resolve_vote(room_code, gen_snapshot). Return {"ok": True}.

    3. Add _resolve_vote(self, room_code: str, expected_generation: int) internal method:
       Under self.lock: get session. If vote_record is None or generation != expected_generation: return (stale timer guard). Cancel timer if set. Count yes_count and total = len(session.players). Compute majority = yes_count > total / 2. Clear session.vote_record = None. If majority: set session.status = "IN_PROGRESS", clear _used_images_this_game[room_code], snapshot broadcast_restart data. Else: set session.status = "ENDED", snapshot broadcast_ended data including final_scores (sorted list of {player_id, player_name, total}) and turn_score_history. Exit lock. If broadcast_restart: broadcast "game_restarting" then call _assign_images_for_turn(room_code) then rebuild TurnMachine (same signature as in start_game(), with on_game_ended=lambda: self._set_session_ended(room_code) and on_scoring_phase=lambda ts: self._accumulate_scores(room_code, ts)). If broadcast_ended: broadcast "game_ended" with final_scores and turn_score_history, then under lock remove session from self.sessions. Handle all failed lists via _remove_failed_players.

    4. Connect _start_vote to the game lifecycle: modify the TurnMachine on_game_ended callback in start_game() to call self._start_vote(room_code) after _set_session_ended — this is the "after last turn" hook specified in CONTEXT.md.

    Then fill in all 6 stubs in tests/test_postgame.py with real pytest assertions. For timer-dependent tests, call _resolve_vote() directly instead of waiting for the real 30s timer.
  </action>
  <verify>
    <automated>python -m pytest tests/test_postgame.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 6 postgame stubs PASSED (or SKIPPED for tests requiring multi-process setup).

    Broadcast capture check — zero lines where broadcast() return is discarded:
    grep -n "broadcaster\.broadcast" server/game_server.py | grep -v "_remove_failed_players"
    (should return 0 matching lines)

    Full suite: python -m pytest tests/ -x -q exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket.IO client → Bridge → GameServer RPC | player_id and room_code arrive from client; must be validated server-side before callback re-registration or vote recording |
| threading.Timer thread → GameServer | _resolve_vote() runs in a timer thread; must not re-enter self.lock deadlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Spoofing | reconnect_player() | mitigate | Validate _player_to_room.get(player_id) == room_code before re-registering callback; unknown UUIDs return {"error": "player not in session"} |
| T-07-02-02 | Tampering | submit_vote() | mitigate | Check `player_id in vote_record.votes` before counting; second submission returns {"ok": True, "duplicate": True} without changing tally |
| T-07-02-03 | Denial of Service | send_chat() message length | mitigate | Truncate message to 200 chars: str(message)[:200] in send_chat() per Pattern 5 |
| T-07-02-04 | Tampering | _remove_failed_players() lock ordering | mitigate | All broadcaster.broadcast() calls after self.lock exits — deadlock prevented by broadcast-outside-lock pattern (verified in all prior phases) |
| T-07-02-05 | Denial of Service | grace period timer leak | accept | Timers are daemon threads; if bridge restarts all dicts are cleared and GameServer retains player until next broadcast failure triggers removal |
</threat_model>

<verification>
- python -m pytest tests/test_session.py::test_host_transfer_on_leave tests/test_event_broadcaster.py tests/test_reconnect.py tests/test_postgame.py tests/test_chat.py -v: all green
- python -m pytest tests/ -x -q exits 0 (no regressions in prior tests)
- grep -n "broadcaster\.broadcast" server/game_server.py | grep -v "_remove_failed_players" returns 0 lines (all broadcast() return values captured)
- grep -n "def reconnect_player\|def send_chat\|def submit_vote\|def _start_vote\|def _resolve_vote\|def _remove_failed_players\|failure_counts\|turn_score_history\|VoteRecord" server/game_server.py server/event_broadcaster.py shows all expected identifiers
</verification>

<success_criteria>
- EventBroadcaster.broadcast() returns list[str] (failed player_ids)
- EventBroadcaster has self.failure_counts dict; 3 consecutive failures trigger inclusion in failed list
- GameSession has turn_score_history (list) and vote_record fields
- VoteRecord dataclass defined near GameSession
- GameServer has: reconnect_player, send_chat, submit_vote, _start_vote, _resolve_vote, _remove_failed_players
- _accumulate_scores appends to turn_score_history each scoring phase
- test_host_transfer_on_leave PASSED (SESSION-07 host transfer confirmed)
- All 17 Phase 7 test stubs PASSED
- Full test suite green
- Zero bare broadcaster.broadcast() calls with discarded return values in game_server.py
</success_criteria>

<output>
Create `.planning/phases/07-reconnection-end-of-game/07-02-SUMMARY.md` when done
</output>
