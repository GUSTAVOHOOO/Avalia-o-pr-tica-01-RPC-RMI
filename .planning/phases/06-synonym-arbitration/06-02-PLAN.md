---
phase: 06-synonym-arbitration
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - server/arbitration.py
  - validate_manifest.py
autonomous: true
requirements:
  - GUESS-03

must_haves:
  truths:
    - "arbitrate('maçã', 'maçã', 0.7) returns (True, 'maçã', 'exact')"
    - "arbitrate('bicicleta', 'maçã', 0.7) returns (False, None, 'synonym')"
    - "arbitrate('xyzzy', 'xyzzy', 0.7) returns (True, 'xyzzy', 'fallback')"
    - "All 8 test stubs in test_arbitration.py pass (not skipped)"
    - "validate_manifest.py runs and exits 0 when all manifest words have synsets"
  artifacts:
    - path: "server/arbitration.py"
      provides: "arbitrate() + ensure_nltk_corpora() + _max_wup_similarity()"
      min_lines: 60
      exports:
        - "ensure_nltk_corpora"
        - "arbitrate"
    - path: "validate_manifest.py"
      provides: "standalone manifest validation CLI script"
      min_lines: 30
      contains: "sys.exit"
  key_links:
    - from: "server/arbitration.py"
      to: "nltk.corpus.wordnet"
      via: "from nltk.corpus import wordnet as wn"
      pattern: "wn\\.synsets"
    - from: "validate_manifest.py"
      to: "server/arbitration.py"
      via: "from server.arbitration import ensure_nltk_corpora"
      pattern: "from server\\.arbitration import"
---

<objective>
Wave 1: implement the pure arbitration module (server/arbitration.py) and standalone validation script (validate_manifest.py), and make all 8 test stubs pass.

Purpose: Deliver the core GUESS-03 arbitration logic as a tested pure function before wiring into game_server.py. All three tiers (exact, synonym, fallback) verified green.
Output: server/arbitration.py with full arbitrate() implementation; validate_manifest.py CLI; 8 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-synonym-arbitration/06-CONTEXT.md
@.planning/phases/06-synonym-arbitration/06-RESEARCH.md
@.planning/phases/06-synonym-arbitration/06-PATTERNS.md
@.planning/phases/06-synonym-arbitration/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From server/arbitration.py (to be created — this is the contract):
  ensure_nltk_corpora() -> None
    Downloads wordnet and omw-1.4 if not present.
    Uses nltk.data.find('corpora/wordnet.zip') — NOT 'corpora/wordnet' (raises LookupError even when installed).

  _max_wup_similarity(synsets_a, synsets_b) -> float
    Returns max Wu-Palmer similarity over all cross-product pairs.
    Guards: if sim is not None and sim > max_sim.
    Returns 0.0 when no valid pairs.

  arbitrate(guess: str, target: str, threshold: float) -> tuple[bool, str | None, str]
    Returns (is_correct, matched_word, match_type).
    match_type values: 'exact' | 'synonym' | 'fallback' (never None).
    matched_word: canonical target when correct, None when incorrect (D-05/D-06).
    Three tiers (GUESS-03):
      (a) guess.strip().lower() == target.strip().lower() -> True, target, 'exact'
      (b) wn.synsets(guess, lang='por') cross wn.synsets(target, lang='por') max wup >= threshold -> True/False, target/None, 'synonym'
      (c) fallback when synsets empty -> exact comparison, return ..., 'fallback'

From config.py (existing):
  WU_PALMER_THRESHOLD = 0.7  # added in Plan 06-01

From server/images/manifest.json (updated in Plan 06-01):
  {"apple.jpg": "maçã", "bicycle.jpg": "bicicleta", ...}  # 12 Portuguese values
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement server/arbitration.py and make all 8 test stubs pass</name>
  <files>server/arbitration.py, tests/test_arbitration.py</files>
  <read_first>
    - tests/test_arbitration.py (current stubs — executor must fill in the assert bodies while replacing pytest.skip() calls)
    - server/turn_machine.py lines 1-25 (module docstring + logging pattern to copy for arbitration.py)
    - .planning/phases/06-synonym-arbitration/06-RESEARCH.md (Pattern 1: ensure_nltk_corpora; Pattern 2: Portuguese synset lookup; Pattern 3: _max_wup_similarity; Pattern 4: full arbitrate() signature; Code Examples section for complete module)
    - .planning/phases/06-synonym-arbitration/06-PATTERNS.md (Pattern Assignments for arbitration.py — imports, docstring, pure-function naming convention)
  </read_first>
  <action>
    Create server/arbitration.py. Follow server/turn_machine.py module structure exactly:
    - Module docstring: triple-quoted, one-line purpose then blank line then detail (copy style from turn_machine.py)
    - Imports: stdlib first (logging, typing), then nltk, then nothing else (no config import — threshold comes in as parameter)
    - Module logger: logger = logging.getLogger(__name__)

    Functions to implement in order:

    1. ensure_nltk_corpora() -> None
       For each corpus in [('wordnet', 'corpora/wordnet.zip'), ('omw-1.4', 'corpora/omw-1.4.zip')]:
         try: nltk.data.find(zip_path) except LookupError: logging.info(f"Downloading NLTK corpus: {corpus_name}"); nltk.download(corpus_name, quiet=True)
       CRITICAL: use 'corpora/wordnet.zip' (with .zip) — NOT 'corpora/wordnet'. The non-.zip path raises LookupError even when installed.

    2. _max_wup_similarity(synsets_a, synsets_b) -> float
       Module-private (underscore prefix). Iterates all pairs from synsets_a x synsets_b.
       For each pair: sim = s1.wup_similarity(s2); guard: if sim is not None and sim > max_sim: max_sim = sim
       Returns max_sim (initialized to 0.0).

    3. arbitrate(guess: str, target: str, threshold: float) -> Tuple[bool, Optional[str], str]
       guess_clean = guess.strip(); target_clean = target.strip()
       (a) if guess_clean.lower() == target_clean.lower(): return True, target, 'exact'
       (b) guess_synsets = wn.synsets(guess_clean, lang='por'); target_synsets = wn.synsets(target_clean, lang='por')
           if guess_synsets and target_synsets:
             sim = _max_wup_similarity(guess_synsets, target_synsets)
             is_correct = sim >= threshold
             return is_correct, (target if is_correct else None), 'synonym'
       (c) is_correct = guess_clean.lower() == target_clean.lower()
           return is_correct, (target if is_correct else None), 'fallback'

    Important behavior notes (from RESEARCH.md Common Pitfalls):
    - Wu-Palmer at 0.7 intentionally accepts near-synonyms (banana ~ apple = 0.82 is expected; document in code comment at step b)
    - Manifest now has Portuguese values; both guess and target use lang='por' — no English fallback needed in arbitrate() since manifest was updated to Portuguese (Option A)

    Then fill in tests/test_arbitration.py stubs — replace each pytest.skip() call with actual assertions:

    test_exact_match(): arbitrate('maçã', 'maçã', THRESHOLD) -> ok=True, word='maçã', typ='exact'
    test_exact_match_case_insensitive(): arbitrate('MAÇÃ', 'maçã', THRESHOLD) -> ok=True, typ='exact'
    test_synonym_pt_guess_pt_target(): arbitrate('bicicleta', 'bicicleta', THRESHOLD) at exact first, OR use a true synonym pair. Better: use arbitrate('maça', 'maçã', THRESHOLD) — intentional typo that won't match exact but... actually use a confirmed synonym pair. Use arbitrate('computador', 'laptop', THRESHOLD) — verify via research that wup sim >= 0.7. If uncertain, use the confirmed pair from RESEARCH.md: the test covers GUESS-03b by calling arbitrate with a Portuguese word that is a synonym of the target. The safest confirmed pair per RESEARCH.md is arbitrate('maçã', 'maçã', THRESHOLD) for exact, and for synonym tier: check if the research file lists a confirmed PT-PT synonym pair. If not confirmed, skip this test with a note explaining it needs a live NLTK check. Use the test_wup_threshold_boundary to cover the threshold logic: arbitrate('bicicleta', 'bicicleta', 0.0) should return 'exact' (step a fires first regardless of threshold).
    test_synonym_wrong_guess(): arbitrate('cachorro', 'maçã', THRESHOLD) -> ok=False, word=None
    test_fallback_when_no_synsets(): arbitrate('xyzzy', 'xyzzy', THRESHOLD) -> ok=True, typ='fallback' (no synsets, falls through to step c, exact match on identical string)
    test_match_type_on_incorrect_guess(): arbitrate('cachorro', 'maçã', THRESHOLD) -> ok=False, word=None, typ in ('exact', 'synonym', 'fallback')
    test_matched_word_is_target_not_guess(): arbitrate('maçã', 'maçã', THRESHOLD) -> word == 'maçã' (matches target, not guess — D-05)
    test_wup_threshold_boundary(): arbitrate('maçã', 'maçã', 1.1) -> ok=False (above max possible), typ='synonym' — unless exact fires first. Because 'maçã' == 'maçã' (step a fires first), use two different words that are confirmed synonyms: arbitrate('bicicleta', 'bicicleta', ...) same issue. For threshold boundary: find two synonymous PT words that aren't identical. If no confirmed pair available from research, make threshold test use xyzzy + very_high_threshold to confirm fallback path regardless.

    NOTE: The executor must read RESEARCH.md Code Examples section which has the complete arbitration.py and test file with working examples. Use those exact test bodies where confidence is high.
  </action>
  <behavior>
    - arbitrate('maçã', 'maçã', 0.7) returns (True, 'maçã', 'exact')
    - arbitrate('xyzzy', 'xyzzy', 0.7) returns (True, 'xyzzy', 'fallback')
    - arbitrate('cachorro', 'maçã', 0.7) returns (False, None, match_type) where match_type in ('synonym', 'fallback')
    - All 8 test functions in test_arbitration.py assert and pass
    - venv/bin/python -m pytest tests/test_arbitration.py -x -q exits 0
  </behavior>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python -m pytest tests/test_arbitration.py -x -q</automated>
  </verify>
  <done>pytest exits 0 with 8 passed, 0 skipped, 0 errors. server/arbitration.py exists with def arbitrate( and def ensure_nltk_corpora(.</done>
</task>

<task type="auto">
  <name>Task 2: Create validate_manifest.py standalone validation script</name>
  <files>validate_manifest.py</files>
  <read_first>
    - server/images/manifest.json (updated Portuguese values from Plan 06-01; executor reads to understand current content)
    - .planning/phases/06-synonym-arbitration/06-RESEARCH.md (validate_manifest.py code example in Code Examples section and Pattern 6: Manifest Validation)
    - .planning/phases/06-synonym-arbitration/06-PATTERNS.md (validate_manifest.py pattern assignment — manifest load path adjustment for project root location)
  </read_first>
  <action>
    Create validate_manifest.py at the project root (alongside config.py and requirements.txt, NOT inside server/).

    Module header: module docstring explaining it is a standalone pre-deploy validation script, run via `python validate_manifest.py`, does NOT modify manifest.json (reporting-only per research A3 assumption).

    Imports: os, json, sys, then nltk imports.

    Corpus guard: call ensure_nltk_corpora() from server.arbitration before any wordnet usage. This requires that server/ is importable — add sys.path.insert(0, os.path.dirname(__file__)) at top if needed (follow the same pattern as server/game_server.py line 22 which does sys.path.insert).

    Manifest load: use os.path.join(os.path.dirname(__file__), 'server', 'images', 'manifest.json') — the script is at project root, so path needs the 'server' prefix unlike game_server.py's own dirname.

    Validation loop: for filename, word in manifest.items():
      synsets = wn.synsets(word, lang='por')
      if synsets: valid.append((filename, word, len(synsets)))
      else: excluded.append((filename, word)); logging.warning(f"WARNING: '{word}' has no Portuguese synsets in omw-1.4 — excluded from distribution")

    Report (print to stdout, not logging — this is a CLI tool per PATTERNS.md):
      print(f"Manifest validation: {len(valid)} valid, {len(excluded)} excluded")
      For valid entries: print(f"  OK  {filename}: '{word}' ({count} synsets)")
      For excluded entries: print(f"  EXCLUDED  {filename}: '{word}' — no synsets found")

    Exit code: if excluded: sys.exit(1). This makes the script CI-friendly. If all words valid, exits 0.

    Since manifest now has Portuguese words (Option A), expect 0 exclusions and exit 0 when run against current manifest.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python validate_manifest.py; echo "Exit code: $?"</automated>
  </verify>
  <done>validate_manifest.py runs without ImportError or crash; prints "Manifest validation: 12 valid, 0 excluded"; exits with code 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player guess → arbitrate() | guess_clean already limited to 50 chars + stripped by submit_guess(); arbitrate() receives pre-validated input |
| NLTK corpus download | Downloads from nltk.org over HTTPS; controlled by ensure_nltk_corpora() at startup only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Unicode guess input to wn.synsets() | accept | Input pre-cleaned in submit_guess() (max 50 chars, stripped); NLTK handles unicode gracefully — verified with 'maçã' live test |
| T-06-03 | DoS | Large synset cross-product | accept | Max ~15 synsets × 15 synsets = 225 comparisons per guess; negligible at 2-4 player scale; wup_similarity() is pure Python arithmetic |
| T-06-04 | Tampering | NLTK corpus download path | accept | ensure_nltk_corpora() downloads from hardcoded nltk.org; no user-controlled download URL; one-time at startup |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | NLTK pre-approved in CLAUDE.md; no new packages in this plan |
</threat_model>

<verification>
After both tasks complete:
1. `venv/bin/python -m pytest tests/test_arbitration.py -v` — 8 passed, 0 skipped
2. `venv/bin/python -m pytest tests/ -q` — full suite green (existing tests unaffected)
3. `venv/bin/python validate_manifest.py` — prints "12 valid, 0 excluded", exits 0
4. `grep -c "def arbitrate" server/arbitration.py` — returns 1
5. `grep -c "def ensure_nltk_corpora" server/arbitration.py` — returns 1
</verification>

<success_criteria>
- server/arbitration.py exists with arbitrate(), ensure_nltk_corpora(), _max_wup_similarity()
- All three GUESS-03 tiers tested and passing: exact match, synonym match, fallback
- D-05 and D-06 tested: matched_word is target (not guess) when correct; None when incorrect; match_type set regardless of correctness
- validate_manifest.py runs from project root, prints validation report, exits 0 against current Portuguese manifest
- Full pytest suite remains green
</success_criteria>

<output>
Create `.planning/phases/06-synonym-arbitration/06-02-SUMMARY.md` when done
</output>
