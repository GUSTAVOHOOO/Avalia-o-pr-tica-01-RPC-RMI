---
phase: 06-synonym-arbitration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - config.py
  - server/images/manifest.json
  - tests/test_arbitration.py
autonomous: true
requirements:
  - GUESS-03

must_haves:
  truths:
    - "pytest discovers tests/test_arbitration.py and reports all stubs as skipped (not error)"
    - "requirements.txt contains nltk==3.9.4"
    - "config.py contains WU_PALMER_THRESHOLD = 0.7"
    - "manifest.json values are Portuguese words (maçã, bicicleta, cadeira, etc.)"
  artifacts:
    - path: "tests/test_arbitration.py"
      provides: "pytest stubs for GUESS-03a, 03b, 03c, D-05, D-06"
      min_lines: 40
    - path: "config.py"
      provides: "WU_PALMER_THRESHOLD constant"
      contains: "WU_PALMER_THRESHOLD = 0.7"
    - path: "server/images/manifest.json"
      provides: "Portuguese object names"
      contains: "maçã"
  key_links:
    - from: "tests/test_arbitration.py"
      to: "server/arbitration.py"
      via: "from server.arbitration import arbitrate, ensure_nltk_corpora"
      pattern: "from server\\.arbitration import"
---

<objective>
Wave 0 infrastructure: update manifest.json to Portuguese words (Option A per D-02), add WU_PALMER_THRESHOLD to config.py, add nltk==3.9.4 to requirements.txt, and create test stubs for all GUESS-03 sub-clauses.

Purpose: Establish the test harness and configuration before implementing arbitration logic. Ensures subsequent plans have a red-first TDD baseline and no missing constants.
Output: Green pytest run (stubs skipped), manifest with Portuguese words, nltk pinned in requirements, threshold in config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-synonym-arbitration/06-CONTEXT.md
@.planning/phases/06-synonym-arbitration/06-RESEARCH.md
@.planning/phases/06-synonym-arbitration/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update manifest.json to Portuguese, add WU_PALMER_THRESHOLD to config.py, add nltk to requirements.txt</name>
  <files>server/images/manifest.json, config.py, requirements.txt</files>
  <read_first>
    - server/images/manifest.json (current state: English values like "apple", "bicycle")
    - config.py (current constants block ending at PHASE_DURATIONS; add after it)
    - requirements.txt (current: 5 lines; add nltk==3.9.4 after simple-websocket line)
  </read_first>
  <action>
    Manifest update (Option A per D-02): change the value side of every key-value pair in server/images/manifest.json from English to Portuguese. Image filenames (keys) remain unchanged. The mapping is:
    - "apple.jpg" -> "maçã"
    - "bicycle.jpg" -> "bicicleta"
    - "chair.jpg" -> "cadeira"
    - "clock.jpg" -> "relógio"
    - "guitar.jpg" -> "violão"
    - "hat.jpg" -> "chapéu"
    - "laptop.jpg" -> "laptop"
    - "umbrella.jpg" -> "guarda-chuva"
    - "book.jpg" -> "livro"
    - "cup.jpg" -> "xícara"
    - "shoe.jpg" -> "sapato"
    - "tree.jpg" -> "árvore"

    config.py addition: after the closing brace of PHASE_DURATIONS, add a blank line then:
    "# Wu-Palmer similarity threshold for synonym arbitration (D-01)."
    "# 0.7 accepts near-synonyms (e.g., bicicleta ~ bike = ~0.9); tune here, never in arbitration.py."
    "WU_PALMER_THRESHOLD = 0.7"
    Follow the existing pattern: inline comment line directly above the constant.

    requirements.txt addition: insert "nltk==3.9.4" on a new line after "simple-websocket==1.1.0" and before "pytest". Maintain the existing one-package-per-line pinned format (runtime deps before test-only deps).
  </action>
  <behavior>
    - config.py imports cleanly: `python -c "import config; print(config.WU_PALMER_THRESHOLD)"` prints 0.7
    - manifest.json is valid JSON with 12 entries whose values are Portuguese strings
    - requirements.txt contains "nltk==3.9.4" as its own line
  </behavior>
  <verify>
    <automated>grep -c "WU_PALMER_THRESHOLD" /home/spacko/projects/faculdade/sd-rpc-av-1/config.py && grep "nltk==3.9.4" /home/spacko/projects/faculdade/sd-rpc-av-1/requirements.txt && grep "maçã" /home/spacko/projects/faculdade/sd-rpc-av-1/server/images/manifest.json</automated>
  </verify>
  <done>config.py grep returns 1 (constant defined), requirements.txt grep returns the line, manifest.json grep finds "maçã"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_arbitration.py with pytest.skip stubs</name>
  <files>tests/test_arbitration.py</files>
  <read_first>
    - tests/test_scoring.py (pure-function test pattern: one assert cluster per test, f-string messages, requirement ID in docstring)
    - tests/test_exchange.py (module header pattern, pytest.skip convention used in phase 05)
    - .planning/phases/06-synonym-arbitration/06-RESEARCH.md (Test file structure section for exact stub function signatures)
    - .planning/phases/06-synonym-arbitration/06-VALIDATION.md (Per-Task Verification Map for required test names)
  </read_first>
  <action>
    Create tests/test_arbitration.py. The file is a pytest stub set — each test function calls pytest.skip() so the suite remains green (skipped, not errored) until Plan 06-02 implements arbitrate().

    Module header (follow tests/test_scoring.py pattern):
    - Triple-quoted docstring: "Unit tests for arbitration module (GUESS-03 coverage)."
    - Import: "import pytest"
    - Import (will resolve after Plan 06-02): "from server.arbitration import arbitrate, ensure_nltk_corpora"
    - Module-level corpus guard call: "ensure_nltk_corpora()" immediately after imports
    - Module constant: "THRESHOLD = 0.7"

    IMPORTANT: Since server/arbitration.py does not exist yet, wrap the import in a try/except ImportError that calls pytest.skip() at collection time if the module is missing. Use the pattern:
      try:
          from server.arbitration import arbitrate, ensure_nltk_corpora
      except ImportError:
          pytest.skip("server.arbitration not yet implemented", allow_module_level=True)

    Stub test functions — each calls pytest.skip("not implemented") inside the body:

    1. test_exact_match() — docstring "GUESS-03a: exact case-sensitive match accepted"
    2. test_exact_match_case_insensitive() — docstring "GUESS-03a: exact match is case-insensitive"
    3. test_synonym_pt_guess_pt_target() — docstring "GUESS-03b: Portuguese guess vs Portuguese target via omw-1.4 synsets"
    4. test_synonym_wrong_guess() — docstring "GUESS-03b: non-synonym guess rejected, matched_word is None"
    5. test_fallback_when_no_synsets() — docstring "GUESS-03c: unknown word triggers fallback path, does not crash"
    6. test_match_type_on_incorrect_guess() — docstring "D-06: match_type is set even on incorrect guesses"
    7. test_matched_word_is_target_not_guess() — docstring "D-05: matched_word is canonical target word, not the guess"
    8. test_wup_threshold_boundary() — docstring "GUESS-03b: score exactly at threshold is accepted; score below is rejected"

    Follow tests/test_scoring.py f-string assertion message style even though assertions will be unreachable until stubs are filled in (include them in comments or as the assert body with a placeholder).
  </action>
  <behavior>
    - pytest collects test_arbitration.py with 8 functions, all skipped (not errored)
    - Running full suite adds 8 skipped to existing count, zero new failures
  </behavior>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python -m pytest tests/test_arbitration.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>pytest output shows 8 items skipped (s), 0 errors, 0 failures — collection succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest.json → game_server | File read at startup; value side now contains Unicode (accented Portuguese) — must open with encoding="utf-8" (already present in __init__) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | manifest.json Unicode values | accept | UTF-8 encoding already enforced in game_server.py json.load(); manifest is local file, not user input |
| T-06-SC | Tampering | nltk==3.9.4 pip install | accept | NLTK is pre-approved in CLAUDE.md; Package Legitimacy Audit in 06-RESEARCH.md confirms [Approved]; no blocking checkpoint needed |
</threat_model>

<verification>
After both tasks complete:
1. `venv/bin/python -m pytest tests/ -q` — full suite green, 8 added skips, zero new failures
2. `python -c "import config; assert config.WU_PALMER_THRESHOLD == 0.7"` — exits 0
3. `python -c "import json; m = json.load(open('server/images/manifest.json', encoding='utf-8')); assert 'maçã' in m.values()"` — exits 0
4. `grep 'nltk==3.9.4' requirements.txt` — returns the line
</verification>

<success_criteria>
- 8 test stubs collected and skipped by pytest (0 errors)
- manifest.json has 12 entries with Portuguese values; image filename keys unchanged
- config.py has WU_PALMER_THRESHOLD = 0.7 following existing comment-above-constant pattern
- requirements.txt has nltk==3.9.4 between simple-websocket and pytest
</success_criteria>

<output>
Create `.planning/phases/06-synonym-arbitration/06-01-SUMMARY.md` when done
</output>
