---
phase: 06-synonym-arbitration
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - server/game_server.py
autonomous: true
requirements:
  - GUESS-03

must_haves:
  truths:
    - "submit_guess() calls arbitrate() instead of the single exact-match line"
    - "GUESS_RESULT broadcast includes matched_word and match_type fields (D-05)"
    - "GameServer.__init__() calls ensure_nltk_corpora() and self._validate_manifest_words() at startup"
    - "Server logs WARNING for any manifest word with zero synsets and excludes it from the image pool"
    - "Full pytest suite green after changes"
  artifacts:
    - path: "server/game_server.py"
      provides: "arbitrate() wired into submit_guess(), _validate_manifest_words() in __init__()"
      contains: "from server.arbitration import arbitrate, ensure_nltk_corpora"
  key_links:
    - from: "server/game_server.py submit_guess()"
      to: "server/arbitration.arbitrate()"
      via: "is_correct, matched_word, match_type = arbitrate(guess_clean, expected, config.WU_PALMER_THRESHOLD)"
      pattern: "arbitrate\\(guess_clean"
    - from: "server/game_server.py __init__()"
      to: "server/arbitration.ensure_nltk_corpora()"
      via: "ensure_nltk_corpora()"
      pattern: "ensure_nltk_corpora"
    - from: "server/game_server.py broadcast_data"
      to: "bridge GUESS_RESULT event"
      via: "matched_word and match_type keys in broadcast_data dict"
      pattern: "matched_word.*match_type"
---

<objective>
Wave 2: wire arbitration into server/game_server.py — replace the single exact-match line in submit_guess() with the arbitrate() call, enrich the GUESS_RESULT broadcast payload with matched_word and match_type (D-05/D-06), and add corpus guard + manifest validation to GameServer.__init__().

Purpose: Complete the vertical slice — arbitration logic is live in the running server. This is the only file change in this plan; arbitration.py and tests are already complete.
Output: game_server.py with arbitrate() wired, GUESS_RESULT enriched, startup validation active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-synonym-arbitration/06-CONTEXT.md
@.planning/phases/06-synonym-arbitration/06-RESEARCH.md
@.planning/phases/06-synonym-arbitration/06-PATTERNS.md
@.planning/phases/06-synonym-arbitration/06-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs from prior plans -->

From server/arbitration.py (created in Plan 06-02):
  ensure_nltk_corpora() -> None
  arbitrate(guess: str, target: str, threshold: float) -> tuple[bool, str | None, str]
    Returns: (is_correct, matched_word_or_None, match_type)
    match_type: 'exact' | 'synonym' | 'fallback'

From config.py (updated in Plan 06-01):
  WU_PALMER_THRESHOLD = 0.7

From server/game_server.py (current state — to be modified):
  Line ~29: from server.turn_state import ExchangeRecord  (add arbitration import after this)
  Lines 87-95: __init__() with self._image_manifest loaded from manifest.json
  Lines 537-572: submit_guess() — exact change point is line 560:
    BEFORE: is_correct = guess_clean.lower() == expected.strip().lower()
    AFTER:  is_correct, matched_word, match_type = arbitrate(guess_clean, expected, config.WU_PALMER_THRESHOLD)
  Lines 564-569: broadcast_data dict — add "matched_word": matched_word, "match_type": match_type
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add arbitration import and _validate_manifest_words() to GameServer.__init__()</name>
  <files>server/game_server.py</files>
  <read_first>
    - server/game_server.py lines 1-100 (full header, imports block, __init__() body — read before touching any line)
    - .planning/phases/06-synonym-arbitration/06-PATTERNS.md (Pattern for __init__() startup additions and imports block — exact line positions)
    - .planning/phases/06-synonym-arbitration/06-RESEARCH.md (Pattern 6: Manifest Validation and _validate_manifest_words() implementation; Pitfall 4: lock held during WordNet I/O — why corpus warmup is needed)
  </read_first>
  <action>
    Two changes to server/game_server.py:

    CHANGE 1 — Import additions:
    After the existing line "from server.turn_state import ExchangeRecord" (currently the last import in the block), add on the next two lines:
      from server.arbitration import arbitrate, ensure_nltk_corpora
      from nltk.corpus import wordnet as wn

    CHANGE 2 — __init__() startup additions:
    After the existing line that assigns self._image_manifest (the json.load line, currently:
      "self._image_manifest: dict = json.load(manifest_file)"), and after "self._used_images_this_game: dict = {}", add:

      # NLTK corpus guard — downloads wordnet and omw-1.4 if not present (D-04).
      # Must run before any wn.synsets() call to avoid lazy-load I/O inside the lock.
      ensure_nltk_corpora()
      # Warm up corpus to avoid first-call latency during GUESS_PHASE (Pitfall 4).
      wn.synsets('teste', lang='por')
      # Filter manifest to words with Portuguese WordNet coverage (D-03, D-04).
      self._image_manifest = self._validate_manifest_words()

    CHANGE 3 — Add _validate_manifest_words() method to GameServer class:
    Add this method after __init__() and before ping(). Follow the exact pattern from RESEARCH.md Pattern 6:

    def _validate_manifest_words(self) -> dict:
        """Return filtered manifest excluding words with no WordNet coverage.

        Logs WARNING for each excluded word. Server starts normally regardless (D-03).
        Must be called after self._image_manifest is loaded and after ensure_nltk_corpora().
        """
        import logging
        valid = {}
        for filename, word in self._image_manifest.items():
            synsets = wn.synsets(word, lang='por')
            if synsets:
                valid[filename] = word
            else:
                logging.warning(
                    f"WARNING: '{word}' has no Portuguese synsets in omw-1.4 "
                    f"— excluded from distribution"
                )
        return valid

    Note: manifest.json now contains Portuguese words (updated in Plan 06-01), so all 12 words should resolve and no exclusions are expected at runtime. The validation is still required per D-04 for defense in depth.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && grep -n "ensure_nltk_corpora\|_validate_manifest_words\|from server.arbitration" server/game_server.py</automated>
  </verify>
  <done>grep finds: "from server.arbitration import arbitrate, ensure_nltk_corpora" in the imports block, "ensure_nltk_corpora()" in __init__(), "self._validate_manifest_words()" in __init__(), and "def _validate_manifest_words(self)" as a method definition</done>
</task>

<task type="auto">
  <name>Task 2: Replace exact-match line in submit_guess() and enrich GUESS_RESULT broadcast</name>
  <files>server/game_server.py</files>
  <read_first>
    - server/game_server.py lines 537-572 (full submit_guess() — read current state before editing; the target line is approximately line 560: "is_correct = guess_clean.lower() == expected.strip().lower()")
    - .planning/phases/06-synonym-arbitration/06-PATTERNS.md (Pattern for submit_guess() replacement — exact before/after delta in the "Core submit_guess() replacement pattern" section)
    - .planning/phases/06-synonym-arbitration/06-RESEARCH.md (Pattern 5: submit_guess() Integration Delta — shows broadcast_data with matched_word and match_type fields)
  </read_first>
  <action>
    Two surgical changes inside submit_guess() in server/game_server.py:

    CHANGE 1 — Replace the arbitration line (currently approximately line 560):
    REMOVE:  is_correct = guess_clean.lower() == expected.strip().lower()
    ADD:     is_correct, matched_word, match_type = arbitrate(
                 guess_clean, expected, config.WU_PALMER_THRESHOLD
             )

    The arbitrate() import is already present from Task 1. config.WU_PALMER_THRESHOLD is available because config is already imported at the top of game_server.py.

    CHANGE 2 — Enrich broadcast_data dict (currently lines 564-569):
    FIND the broadcast_data assignment inside the with self.lock: block. It currently reads:
        broadcast_data = {
            "room_code": room_code,
            "guesser_id": player_id,
            "target_player_id": target_player_id,
            "is_correct": is_correct,
        }
    REPLACE with:
        broadcast_data = {
            "room_code": room_code,
            "guesser_id": player_id,
            "target_player_id": target_player_id,
            "is_correct": is_correct,
            "matched_word": matched_word,   # D-05: canonical target when correct, None otherwise
            "match_type": match_type,       # D-05/D-06: 'exact'|'synonym'|'fallback'
        }

    No other changes to submit_guess(). The broadcaster.broadcast() call remains after the with block exits (broadcast-outside-lock pattern is unchanged). The return dict {"ok": True, "is_correct": is_correct} is unchanged — matched_word and match_type go into broadcast only, not the return value.

    After making changes, run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/spacko/projects/faculdade/sd-rpc-av-1 && venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Full pytest suite exits 0. grep confirms: "arbitrate(" present in game_server.py, "matched_word" present in game_server.py, "match_type" present in game_server.py. No existing test regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submit_guess() → arbitrate() | guess_clean already stripped/limited to 50 chars before arbitrate() call; arbitrate() is a pure function with no I/O after corpus warmup |
| self.lock → arbitrate() call | arbitrate() is called inside with self.lock; WordNet corpus is pre-warmed in __init__() to avoid lazy-load I/O blocking the lock (Pitfall 4 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | guess input to arbitrate() | accept | Input pre-cleaned: str(guess_word).strip()[:50] at line 558 before arbitrate() call; no new attack surface |
| T-06-06 | DoS | WordNet lazy-load inside lock | mitigate | ensure_nltk_corpora() + wn.synsets('teste', lang='por') warmup in __init__() before server accepts connections — corpus fully loaded before first guess |
| T-06-07 | Information Disclosure | matched_word in GUESS_RESULT | accept | matched_word is the canonical target word — already revealed when is_correct=True; all players receive GUESS_RESULT; no new disclosure beyond existing design |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; nltk already added in Plan 06-01 |
</threat_model>

<verification>
After both tasks complete:
1. `venv/bin/python -m pytest tests/ -q` — full suite green, 0 failures
2. `grep -n "arbitrate(" server/game_server.py` — finds the call in submit_guess()
3. `grep -n "matched_word\|match_type" server/game_server.py` — finds both in broadcast_data
4. `grep -n "ensure_nltk_corpora\|_validate_manifest_words" server/game_server.py` — finds both in __init__()
5. Manual smoke test (optional, for confidence): start server, submit a guess via bridge, observe GUESS_RESULT broadcast contains matched_word and match_type fields
</verification>

<success_criteria>
- server/game_server.py imports arbitrate and ensure_nltk_corpora from server.arbitration
- submit_guess() uses arbitrate(guess_clean, expected, config.WU_PALMER_THRESHOLD) — single exact-match line removed
- GUESS_RESULT broadcast payload contains matched_word and match_type (D-05/D-06)
- GameServer.__init__() calls ensure_nltk_corpora(), warms up corpus, calls _validate_manifest_words()
- Full pytest suite green (all prior tests pass, no regressions)
- Phase 6 ROADMAP success criteria 2 and 3 are now implementable by running the server
</success_criteria>

<output>
Create `.planning/phases/06-synonym-arbitration/06-03-SUMMARY.md` when done
</output>
